### 主播关键词管理接口测试
### 作者: jxy
### 创建时间: 2026-03-25
### 描述: 主播关键词增删改查 API 测试用例

### ========================================
### 基础配置
### ========================================

@baseUrl = http://localhost:8080
@contentType = application/json

### ========================================
### 1. 查询主播关键词列表
### ========================================

### 1.1 正常查询 - 根据主播secUid查询关键词列表
GET {{baseUrl}}/replay/anchorCruxWords/listBySecUid?secUid=test_sec_uid_001
Content-Type: {{contentType}}

### 1.2 查询不存在的主播
GET {{baseUrl}}/replay/anchorCruxWords/listBySecUid?secUid=not_exist_sec_uid
Content-Type: {{contentType}}
# 期望返回空列表

### 1.3 缺少secUid参数
GET {{baseUrl}}/replay/anchorCruxWords/listBySecUid
Content-Type: {{contentType}}
# 期望返回参数错误

### ========================================
### 2. 添加主播关键词
### ========================================

### 2.1 正常添加 - 单个关键词
POST {{baseUrl}}/replay/anchorCruxWords/addKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "keywords": ["促单"]
}

### 2.2 正常添加 - 多个关键词
POST {{baseUrl}}/replay/anchorCruxWords/addKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "keywords": ["互动", "直播", "带货"]
}

### 2.3 添加已存在的关键词（重复添加测试）
POST {{baseUrl}}/replay/anchorCruxWords/addKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "keywords": ["促单"]
}
# 期望：不会重复添加

### 2.4 添加空白关键词（数据校验测试）
POST {{baseUrl}}/replay/anchorCruxWords/addKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "keywords": ["", "  ", "有效关键词"]
}
# 期望：只添加有效关键词，空白被过滤

### 2.5 缺少secUid（校验测试）
POST {{baseUrl}}/replay/anchorCruxWords/addKeywords
Content-Type: {{contentType}}

{
  "keywords": ["测试关键词"]
}
# 期望：返回 "主播secUid不能为空"

### 2.6 缺少keywords（校验测试）
POST {{baseUrl}}/replay/anchorCruxWords/addKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001"
}
# 期望：返回 "关键词列表不能为空"

### 2.7 keywords为空数组（校验测试）
POST {{baseUrl}}/replay/anchorCruxWords/addKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "keywords": []
}
# 期望：返回 "关键词列表不能为空"

### 2.8 包含特殊字符的关键词
POST {{baseUrl}}/replay/anchorCruxWords/addKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_002",
  "keywords": ["买一送一!", "限时优惠~", "9.9包邮"]
}

### ========================================
### 3. 移除主播关键词
### ========================================

### 3.1 正常移除 - 单个关键词
### 注意：keywordId 需要根据实际数据替换
POST {{baseUrl}}/replay/anchorCruxWords/removeKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "keywordIds": [1]
}

### 3.2 正常移除 - 多个关键词
POST {{baseUrl}}/replay/anchorCruxWords/removeKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "keywordIds": [2, 3, 4]
}

### 3.3 移除不存在的关键词ID
POST {{baseUrl}}/replay/anchorCruxWords/removeKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "keywordIds": [999999, 888888]
}
# 期望：返回成功，但移除数量为0

### 3.4 缺少secUid（校验测试）
POST {{baseUrl}}/replay/anchorCruxWords/removeKeywords
Content-Type: {{contentType}}

{
  "keywordIds": [1, 2]
}
# 期望：返回 "主播secUid不能为空"

### 3.5 缺少keywordIds（校验测试）
POST {{baseUrl}}/replay/anchorCruxWords/removeKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001"
}
# 期望：返回 "关键词ID列表不能为空"

### 3.6 keywordIds为空数组（校验测试）
POST {{baseUrl}}/replay/anchorCruxWords/removeKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "keywordIds": []
}
# 期望：返回 "关键词ID列表不能为空"

### 3.7 keywordIds包含null值（边界测试）
POST {{baseUrl}}/replay/anchorCruxWords/removeKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "keywordIds": [1, null, 2]
}
# 期望：过滤null值，移除有效的关键词

### ========================================
### 4. 完整业务流程测试
### ========================================

### 4.1 完整流程：添加 -> 查询 -> 移除 -> 再查询
### Step 1: 添加关键词
POST {{baseUrl}}/replay/anchorCruxWords/addKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_flow",
  "keywords": ["流程测试关键词1", "流程测试关键词2"]
}

### Step 2: 查询确认添加成功
GET {{baseUrl}}/replay/anchorCruxWords/listBySecUid?secUid=test_sec_uid_flow
Content-Type: {{contentType}}

### Step 3: 移除其中一个关键词（需要根据实际返回的ID调整）
POST {{baseUrl}}/replay/anchorCruxWords/removeKeywords
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_flow",
  "keywordIds": [1]
}

### Step 4: 再次查询确认移除成功
GET {{baseUrl}}/replay/anchorCruxWords/listBySecUid?secUid=test_sec_uid_flow
Content-Type: {{contentType}}

### ========================================
### 响应格式说明
### ========================================

### 成功响应格式（查询）：
# {
#   "code": 0,
#   "msg": "获取成功",
#   "data": [
#     {
#       "id": 1,
#       "secUid": "test_sec_uid_001",
#       "keyword": "促单",
#       "userId": 1001,
#       "tenantId": 1,
#       "createDate": "2026-03-25 15:00:00",
#       "updateDate": "2026-03-25 15:00:00"
#     }
#   ]
# }

### 成功响应格式（添加）：
# {
#   "code": 0,
#   "msg": "添加成功",
#   "data": null
# }

### 成功响应格式（移除）：
# {
#   "code": 0,
#   "msg": "移除成功，共移除2个关键词",
#   "data": null
# }

### 失败响应格式：
# {
#   "code": 3001,
#   "msg": "主播secUid不能为空",
#   "data": null
# }

### ========================================
### 测试数据准备 SQL
### ========================================

### 清理测试数据：
# DELETE FROM tb_anchor_crux_words_rela WHERE sec_uid LIKE 'test_%';
# DELETE FROM tb_anchor_crux_words WHERE keyword LIKE '流程测试%' OR keyword LIKE '%测试关键词%';

### 准备测试主播数据（如果需要）：
# INSERT INTO tb_anchor_url (id, sec_uid, anchor_name, platform, create_date)
# VALUES (1, 'test_sec_uid_001', '测试主播1', 0, NOW());

### 准备测试关键词数据：
# INSERT INTO tb_anchor_crux_words (id, keyword, create_date, update_date, is_deleted)
# VALUES 
#   (1, '促单', NOW(), NOW(), 0),
#   (2, '互动', NOW(), NOW(), 0),
#   (3, '直播', NOW(), NOW(), 0);

### 准备测试关联数据：
# INSERT INTO tb_anchor_crux_words_rela (id, sec_uid, keyword_id, user_id, tenant_id, create_date, update_date, is_deleted)
# VALUES 
#   (1, 'test_sec_uid_001', 1, 1001, 1, NOW(), NOW(), 0),
#   (2, 'test_sec_uid_001', 2, 1001, 1, NOW(), NOW(), 0);

### ========================================
### 数据库校验说明
### ========================================

### 添加关键词后校验：
# 1. tb_anchor_crux_words 表应新增关键词记录（如果关键词不存在）
# 2. tb_anchor_crux_words_rela 表应新增关联记录
# 3. 校验SQL：
#    SELECT * FROM tb_anchor_crux_words WHERE keyword = '促单';
#    SELECT * FROM tb_anchor_crux_words_rela WHERE sec_uid = 'test_sec_uid_001';

### 移除关键词后校验：
# 1. tb_anchor_crux_words_rela 表对应记录的 is_deleted 应更新为 1
#    或记录被物理删除
# 2. 校验SQL：
#    SELECT * FROM tb_anchor_crux_words_rela 
#    WHERE sec_uid = 'test_sec_uid_001' AND keyword_id IN (1, 2, 3);

### ========================================
### 权限测试说明
### ========================================

### 测试场景：
# 1. 主账号添加关键词 -> 应成功
# 2. 子账号为主播添加关键词 -> 应成功（关联到对应租户）
# 3. 不同租户用户尝试操作其他租户的主播 -> 应失败或无权限
# 4. 未登录用户尝试操作 -> 应返回未授权错误

### ========================================
### 5. 设置主播行业接口测试
### ========================================

### 5.1 正常设置 - 只设置系统行业
POST {{baseUrl}}/replay/words/anchorUrl/setAnchorTrade
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "systemTradeId": 1
}

### 验证要点:
### - 返回 R<String>，code=0
### - 数据库 tb_anchor_url 中 system_trade_id 更新为 1
### - 数据库 tb_anchor_url 中 update_system_trade_date 自动更新为当前时间

### 5.2 正常设置 - 只设置AI纠正行业
POST {{baseUrl}}/replay/words/anchorUrl/setAnchorTrade
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "aiCorrectTradeId": 2
}

### 验证要点:
### - 返回 R<String>，code=0
### - 数据库 tb_anchor_url 中 ai_correct_trade_id 更新为 2
### - 数据库 tb_anchor_url 中 add_ai_trade_date 自动更新为当前时间

### 5.3 正常设置 - 同时设置系统行业和AI纠正行业
POST {{baseUrl}}/replay/words/anchorUrl/setAnchorTrade
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001",
  "systemTradeId": 3,
  "aiCorrectTradeId": 4
}

### 验证要点:
### - 返回 R<String>，code=0
### - 数据库 tb_anchor_url 中 system_trade_id 更新为 3
### - 数据库 tb_anchor_url 中 ai_correct_trade_id 更新为 4
### - 数据库 tb_anchor_url 中 update_system_trade_date 和 add_ai_trade_date 同时更新

### 5.4 缺少secUid（校验失败）
POST {{baseUrl}}/replay/words/anchorUrl/setAnchorTrade
Content-Type: {{contentType}}

{
  "systemTradeId": 1
}

### 验证要点:
### - 返回校验失败提示 "主播secUid不能为空"

### 5.5 两个行业ID都不传（校验失败）
POST {{baseUrl}}/replay/words/anchorUrl/setAnchorTrade
Content-Type: {{contentType}}

{
  "secUid": "test_sec_uid_001"
}

### 验证要点:
### - 返回错误提示 "系统行业ID和AI纠正行业ID至少传一个"

### 5.6 设置不存在的主播
POST {{baseUrl}}/replay/words/anchorUrl/setAnchorTrade
Content-Type: {{contentType}}

{
  "secUid": "not_exist_sec_uid",
  "systemTradeId": 1
}

### 验证要点:
### - 主播不存在时不执行更新，返回成功但不报错

### ========================================
### 设置主播行业数据库校验说明
### ========================================

### 写操作后的数据库验证要点：
# 【设置系统行业】执行后检查：
#   - 表：tb_anchor_url
#   - 验证：system_trade_id 已更新为请求值
#   - 验证：update_system_trade_date 已更新为当前时间
#   - 验证：update_date 已更新
#
# 【设置AI纠正行业】执行后检查：
#   - 表：tb_anchor_url
#   - 验证：ai_correct_trade_id 已更新为请求值
#   - 验证：add_ai_trade_date 已更新为当前时间
#   - 验证：update_date 已更新
#
# 校验SQL：
# SELECT sec_uid, system_trade_id, ai_correct_trade_id, update_system_trade_date, add_ai_trade_date, update_date
# FROM tb_anchor_url WHERE sec_uid = 'test_sec_uid_001';
