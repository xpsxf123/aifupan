<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiuyu.replay.power.repository.dao.UserDetailsDao">

    <select id="countClientDetailBySaleId" resultType="java.lang.Long">
        SELECT count(*)
        FROM tb_user_details ud
                 INNER JOIN tb_user u on ud.user_id = u.id and u.is_deleted = 0 and u.user_type in (0, 2)
        WHERE ud.sale_id = #{saleId}
    </select>
</mapper>
