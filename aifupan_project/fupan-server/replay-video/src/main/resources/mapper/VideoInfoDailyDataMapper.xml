<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiuyu.replay.video.project.dao.VideoInfoDailyDataDao">

    <!-- 查询每个视频最近的同步数据 -->
    <select id="getLatestDailyDataByVideoIds"
            resultType="com.jiuyu.replay.video.project.entity.VideoInfoDailyDataEntity">
        SELECT t1.*
        FROM tb_video_info_daily_data t1
        INNER JOIN (
        SELECT video_id, MAX(data_date) as max_date
        FROM tb_video_info_daily_data
        WHERE video_id IN
        <foreach collection="videoIds" item="videoId" open="(" separator="," close=")">
            #{videoId}
        </foreach>
        AND data_date &lt; #{currentDate}
        GROUP BY video_id
        ) t2 ON t1.video_id = t2.video_id AND t1.data_date = t2.max_date
        ORDER BY t1.video_id
    </select>

</mapper>
