<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jiuyu.replay.order.repository.dao.UserPropertyDetailsDao">
    
    <select id="sumTotalTokensByTenant" resultType="java.lang.Long">
        select sum(c.quantity) as total_tokens from tb_order as a
                join tb_order_detail as b on a.id = b.order_id
                join tb_user_property_details as c on c.order_detail_id = b.id
        where a.user_id = #{mainUserId} and c.commodity_type_code = 'aiTokenNum' and c.signs = 0
    </select>
</mapper>
