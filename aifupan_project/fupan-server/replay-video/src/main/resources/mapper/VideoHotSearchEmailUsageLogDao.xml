<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jiuyu.replay.video.project.dao.VideoHotSearchEmailUsageLogDao">
    
    <select id="loadTimeoutUse" resultType="com.jiuyu.replay.video.project.entity.VideoHotSearchEmailUsageLogEntity">
        select b.* from tb_video_hot_search_email_account as a
               join tb_video_hot_search_email_usage_log as b on a.id = b.email_account_id
         where b.id &gt; #{idx} and DATE_ADD(b.use_time, INTERVAL a.use_timeout_minutes MINUTE) &lt; now() and b.release_time is null
         order by b.id limit #{limit}
    </select>
    
    <update id="batchRelease">
        update tb_video_hot_search_email_usage_log set release_time = now() where id in
        <foreach item="item" collection="ids" separator="," open="(" close=")" index="index">
            #{item}
        </foreach>
    </update>
</mapper>
