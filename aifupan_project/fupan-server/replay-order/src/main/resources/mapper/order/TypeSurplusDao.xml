<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jiuyu.replay.order.repository.dao.TypeSurplusDao">
    
    <select id="currentSurplusCount" resultType="java.lang.Long">
        SELECT SUM(t.use_number) AS sum_use_number
        FROM tb_type_surplus t
        WHERE t.use_status IN (0, 1)
        AND t.time_status = 0
        AND t.commodity_type_id = #{commodityTypeId}
        AND t.property_id = #{propertyId}
    </select>
</mapper>