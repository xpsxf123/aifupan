# Archive Index

This area stores cold documents for traceability.

Hard rules:
- After stable knowledge is extracted during `Archive`, the original `openspec.md` SHOULD move here (renamed with a date prefix).
- Archived documents are read-only. Do not use this folder as active working memory.

## Entries

| 日期 | 摘要 | 链接 |
|---|---|---|
~~| 2026-05-11 | CRM × 销售智能体系统对接 openspec（PRD + 技术架构，含双向同步/事件推送/批量查询接口契约） | 20260511_crm_agent_integration_openspec.md |~~ → harvested 2026-05-20 → wiki/api/crm_api.md, wiki/data/crm_data.md, wiki/domain/crm_domain.md (REDUNDANT — content already in wiki)
~~| 2026-05-14 | CRM Spec-A 查询 API 实现计划（批量聚合查询 + 手机号订单查询，TDD 任务拆解） | 20260514_crm_spec_a_query_apis.md |~~ → harvested 2026-05-20 → wiki/api/crm_api.md (REDUNDANT — APIs C01/C02 already in wiki)
~~| 2026-05-14 | CRM Spec-B 画像写回实现计划（`tb_user_details` 字段同步 + `tb_crm_ai_profile` 新增，TDD 任务拆解） | 20260514_crm_spec_b_profile_writeback.md |~~ → harvested 2026-05-20 → wiki/api/crm_api.md, wiki/data/crm_data.md (REDUNDANT — APIs R01/R02 and tb_crm_ai_profile already in wiki)
~~| 2026-05-14 | CRM Spec-C 阶段事件回传实现计划（`tb_crm_stage_event` 入库 + 受控快照刷新，TDD 任务拆解） | 20260514_crm_spec_c_stage_event.md |~~ → harvested 2026-05-20 → wiki/data/crm_data.md §五 (PRIMARY — stage snapshot whitelist merged)
~~| 2026-05-14 | CRM Spec-D 订单事件出站实现计划（Spring 事件 + 监听器聚合推送智能体，TDD 任务拆解） | 20260514_crm_spec_d_order_event.md |~~ → harvested 2026-05-20 → wiki/api/crm_api.md §三 (PRIMARY — crm.agent config keys merged)
~~| 2026-05-15 | Fix: `tb_crm_*` 缺 `create_id`/`update_id` 导致 HTTP 500（取消继承 `BaseEntity`） | 2026-05-15__fix_crm_table_missing_create_id_update_id.md |~~ → harvested 2026-05-20 → wiki/data/crm_data.md §四 原则5 (PRIMARY — no-BaseEntity constraint merged)
~~| 2026-05-15 | SalesCoach 联调真实数据整理文档（客户 + 销售原始文本 → Markdown，不涉及代码变更） | 2026-05-15__crm_salescoach_fixture_data_doc_openspec.md |~~ → harvested 2026-05-20 → (REDUNDANT — fixture data doc, no stable knowledge)
| 2026-05-20 | **PATCH** — Implement 阶段 skill 加载策略重构：push→pull、L1–L7 SSOT 归并到 CLAUDE.md §5、16 条 checklist 按文件类型裁剪为 4 张微表、新增 `[Sibling Reference]` 硬步骤；修正 dispatch-template 中与项目 DI 约束矛盾的 `@Resource` 例子。改动 3 文件：`lead-engineer.md` / `skill-precedence.md` / `dispatch-template.md`。(no spec — PATCH mode) |
| 2026-05-20 | **PATCH #2** — 清理 DI 注解 SSOT 漂移：`lead-engineer.md` / `code-reviewer.md` / `security-sentinel.md` 中残留的 `@Resource` 推荐改为"构造器注入"，与 CLAUDE.md §5 对齐；security-sentinel grep 用 `\b` 边界排除 `@ResourceLock` 误伤。(no spec — PATCH mode) |
| 2026-05-20 | **PATCH #3** — 兑现 lifecycle.md Part 6 的 failure-memory 注入承诺：新增 `failure_memory.py`（按 14 天窗口从本 index 提取 Fix/Hotfix/修复/回炉 条目，注入 ≤3 条到 stderr，30min cooldown + QUIET 开关 + 静默降级），`user_prompt_submit_hook.py` sibling import 接入。(no spec — PATCH mode) |
| 2026-05-20 | **PATCH #4** — failure-memory 数据源扩 archive ∪ `incidents/*.md`：建立 incidents/ 目录（README 约定 + _TEMPLATE 模板），`failure_memory.py` 加 `_scan_incidents()` + frontmatter 解析（stdlib only），输出按日期 merge，source 标 `[archive]` / `[incident@<area>]`。关闭"事故只靠人工归档"的自选择偏差闭环。(no spec — PATCH mode) |
| 2026-05-20 | **PATCH #5** — incident 文件自动萃取两条入口：(A) `@debugger` Phase 1 加 `Source` 字段，Output 加 `[Incident Draft]` 块（source≠dev-found 时 emit；debugger 不写盘，Hard Limit L150 保留，由 main agent 凭用户确认门写盘）；(B) lifecycle.md Shortcuts 新增 `@incident` 简捷符，main agent inline 从日志萃取 → draft → 用户确认 → 写盘。incidents/README.md 补两条入口的完整工作流 + 反编造红线（`<待补充>` 优于虚构根因）。(no spec — PATCH mode) |
| 2026-05-20 | **PATCH #6** — scope_guard 三连 bug 修复（之前 5 轮 PATCH 的 scope 锁全部裸奔）：Bug 1 `find_active_focus_card.py` 加 PATCH 路径回退 `.claude/workflow/artifacts/focus_card.md` + 模板状态检测（仅 `## Allowed Scope` 有非占位 `-` 条目时视为 active）；Bug 2 `scope_guard.py` 含 `/` 路径不再强加尾 `/` 当前缀，按 `allowed_exact` 处理（恢复单文件 scope 表达力）；Bug 3 scope_guard 加 `_to_repo_relative()` 归一化绝对路径（PreToolUse hook 传的是绝对路径，allowed 列表是相对路径，原先 `startswith` 永远 mismatch）。端到端 6 个场景验证全 PASS（in-scope/out-of-scope/template/prefix 向后兼容）。(no spec — PATCH mode) |
| 2026-05-23 | **PATCH #7** — 修 commands AskUserQuestion 选项数越界（工具硬上限 2-4，原先 5-6 选项会被运行时拒）：`/h-from-ticket` step 3 不可识别分类的 6 选 1 拆成 Q1 4 选 1（Change/PRD/Bug/Special）+ Q2 3 选 1（Security/Compliance/Other，仅 Special 时发）；`/h-collab` step 3 type 推不出的 5 选 1 拆成 Q1 4 选 1（API/Process/Data exchange/Custom）+ Q2 2 选 1（内部映射/外部集成，仅 Data exchange 时发）。两处均补 "禁单题 ≥5 选项 / 禁静默 default" 硬约束。(no spec — PATCH mode) |
| 2026-05-23 | **PATCH #8** — 起草统一 `clarification-protocol.md`（≤4 选项 / ≤4 题 / 禁 Y/n 自然语言确认 / sub-agent 禁 ask / 不可静默 default + 5 段反例→正例），并把 `/h-incident` 字段澄清重构合规：step 2 将散落 4 处自然语言 "推不出 → 问用户" 收敛为 AskUserQuestion 多题（Q1 severity 4 选 1 + Q2 source 4 选 1，其中 source 原 5 个 enum 通过"其他 → Q3 follow-up 2 选 1 (post-mortem / rca-archive)" 拆解）+ area/slug 自由文本一次性合并问；step 5/6 写盘确认 `Y / n / 改 <字段>` 三档自然语言换 AskUserQuestion 3 选 1（写盘 / 取消 / 改字段后重展示），"改字段" follow-up 4 选 1 + 重展示 ≤2 轮硬限。(no spec — PATCH mode) |
| 2026-05-23 | **PATCH #9** — clarification-protocol references 传播：把 `> 人机澄清遵循 .claude/rules/clarification-protocol.md` 引用行追加到 12/13 个 commands（h-archive / h-brief / h-collab / h-collab-update / h-decompose / h-design / h-fix-bug / h-from-ticket / h-gates / h-incident / h-pr / h-research），只读的 h-resume 跳过（无澄清需求）。h-gates 引用行额外标注「FAIL→incident 草稿环节的 Y/n/改字段 自然语言确认是 P1 待消项，按 /h-incident 同款重构」。secrets_linter PASS。(no spec — PATCH mode) |
| 2026-05-23 | **PATCH #10** — 兑现 PATCH #9 标注的 P1 待消项：`/h-gates` step 5 FAIL→incident 草稿写盘确认从 `写盘? (Y / n / 改 <字段>)` 自然语言三档换成 AskUserQuestion 3 选 1（写盘 / 取消 / 改字段后重展示）+ Q2 follow-up 4 选 1（frontmatter / 正文段 / 标题行 / 重新起草整篇）+ 改字段循环 ≤2 轮硬限。同时移除 step 9 引用行里"P1 待消项"标注；硬约束行从 "用户 Y 才 Write" 改为 "走 AskUserQuestion 选 `写盘` 才 Write、禁 Y/n"。这样 `/h-gates` 与 `/h-incident` 写盘协议彻底对齐。(no spec — PATCH mode) |
| 2026-05-24 | **PATCH #11** — 修 AI 模块 wiki 4 处事实偏差（源自 research G4，话术助手调研发现）：(a) `ai_api.md` line 24 "本模块未使用 SSE" 错误描述 → 改为 "replay-ai controller 不用 SSE，但 SSE 服务端入口在 replay-api `AiRelatedController#ask` / `chatStreamProxy`"；(b) `ai_domain.md` 两处 "14 种 askType" → "16 种 (code 0-15)"；(c) `ai_domain.md` §四 useSourceType 单层枚举表 → 拆成两层语义（直接对话写 sourceType 0/1/2/3/5 / 异步增强写 CONVERSATION=6）含代码点；(d) `ai_domain.md` §六.1 提示词来源回退加注 `tb_cue_words` schema 支持 `tenant_id ≠ 0` 租户私有 prompt（DB 数据实证，衍生 G3.1 待开发查 `CueWordsBll` 实际 WHERE）。顺手修 research 报告 3 处 "ai_architecture.md" 指错引用 → 改为 `ai_api.md`。所有 gate PASS（wiki_linter 0 oversize/0 dead links / secrets PASS）。 (no spec — PATCH mode) |
| 2026-05-24 | **PATCH #12** — 把 [research askType-inventory 伴生文档](../wiki/research/20260524__cue-words-askType-inventory.md) 的全 askType 存储分布 + 字段语义回填 wiki：(a) `ai_domain.md` §四 askType 表加"提示词存储"列，明示 11 种走 tb_cue_words / 3 种走 systemKv（9 `suggest_trade_ai_model` / 14 `check_ai_content_prompt` / 15 `correct_ai_content_prompt`）/ 1 种代码硬编码（11 `AnchorUrlLogicImpl.java:978`）/ 1 种死代码（10 EXTRACT_VIDEO 全代码 0 引用），含每种 tb_cue_words 实际条数；(b) `ai_domain.md` §六.1 提示词来源回退新增"重要 2"提示 systemKv/硬编码/死代码三条线；(c) `words_data.md` §5 `tb_cue_words` 行扩展字段语义（accountType 推断为平台账号变体 / syncScene 1-3 同步场景 / applyTo 0-1 多场聚合 / scope 0-1 直播vs短视频 / scene/resourceType 推断为死字段）+ 指向伴生文档。所有 gate PASS。衍生待澄清：G3.3 (accountType 实际语义 PM/开发) / G3.4 (EXTRACT_VIDEO 状态) / G3.5 (scene/outline 字段用途)。 (no spec — PATCH mode) |
| 2026-05-30 | **STANDARD — B4 修补包-2（话术智能监控 code-review 11 项修复）** — 修复 3 CRITICAL + 4 MAJOR + 4 MINOR：C1 `settleAiToken` 改传 `userId`（`ScriptMonitorReportEntity` 加 `userId` 字段 + DDL `sql/replay-26.sql`）；C2 `loadCueWords` 改调新 SPI `CueWordsFeign.getMonitorPromptsByType` 不清空 `problem`（新增 `CueWordsProducer.listMonitorPrompts` + `CueWordsFeign` 接口 + `CueWordsApi` 实现）；C3 `@Transactional` self-call 修复（新建 `ScriptMonitorReportWriteService` 接口 + Impl，`ScriptMonitorGenerateBll` 注入走代理）；M4 `batchReportStatus` 补 `sourceType=1` 分支；M5 `triggerReport` 调整顺序（先 withhold 再 createGeneratingTask）；M6 `@NoRepeatSubmit` key 加 `userId`；M7 `getClientWrapper` N+1 改单次批量 IN 查询；M8 注释去重；M9 新建 `ConfirmRoleEnum` 替换魔法数字；M10 `buildSummary→buildListSummaryText`；M11 `SimpleDateFormat→DateTimeFormatter`。单测全 39 通过。(no spec — PATCH mode) |
| 2026-06-02 | **STANDARD MEDIUM** | B5 监控位资产管理 — 3 个 commodityTypeCode (scriptQualityNum/scriptFidelityNum/interactionPatrolNum, sub_account_have=0) + sql/replay-28.sql 种子数据 INSERT IGNORE + MonitorPositionAuthVo (hasAuth/hasSurplus 加 @JsonIgnore) + UserPropertyFeign.checkMonitorPosition SPI (主子账号回退) + anchorBasicConfig (走 ScriptMonitorBll → AnchorUrlUserFeign 跨模块 SPI) + monitorPositionStatistics + AnchorUrlUserVo 加 4 字段 (3 监控开关 + standardScriptId) + ScriptMonitorBll:387 清 TODO 注释 (手动按 PRD R-P0-003 不校监控位). e2e 11 ACs 全过, mvn -am compile 14 modules SUCCESS, commit 97cc81876. | [link](./20260602_b5-monitor-position.md) |
| 2026-06-02 | **STANDARD MEDIUM** | B6 addOrUpdateAnchor 接入三个 AI 监控开关 — AddOrUpdateAnchorBo + AnchorUrlUserBo 各加 4 字段 (3 开关 + standardScriptId) + AnchorUrlUserProducer.countOpenSwitch SPI (switch case 三分支统计) + AnchorUrlBll.addOrUpdateAnchor 5-Step 校验链 (账号切换 70007 / 还原度暂禁 70005 / 监控位 70002 / Token 70001 / 占用门控 + Feign 返 false 抛 RuntimeException 触发事务回滚 + 4 处 throw log.warn) + null-skip 防误关 + 三能力独立计数门控. 新建 replay-words 单测基建 (pom + AnchorUrlBllTest 9 用例全过). e2e 6/8 ACs (AC-001/002/004/006/007/008) + 2/8 单测覆盖 (AC-003/005). mvn -am compile 14 modules SUCCESS, commit 963764cf7. | [link](./20260602_b6-anchor-switch.md) |
| 2026-06-02 | **STANDARD HIGH** | B7 录制完成自动触发质检/巡检 + MQ 业务直发改造 — ScriptMonitorFeign + ScriptMonitorApi (replay-generic/replay-ai 跨模块 SPI) + AnchorVideoLogicImpl.updateVideoAnalysisStatus 末尾追加纯同步段 (与主播关键词共享 infoByVideoId 查询) + ScriptMonitorBll.autoTriggerReport (跳用户态校验 / BusinessException 降 warn / 其他 error) + handleExisting 覆写 trigger_source='manual' / createGeneratingTaskAndReturn 加 triggerSource 参数 + RocketMqBll.syncSendAndDeliverToTopic (业务直发 RocketMQ + 失败 catch 保 status=0 让 XXL-Job 兜底) + ScriptMonitorBll:441 改新方法 (B4 手动 triggerReport 5min→秒级) + AnchorUrlUserFeign.getBySecUidAndUser SPI + sql/replay-29.sql ADD COLUMN trigger_source VARCHAR(16) NOT NULL DEFAULT 'manual'. 2 ADR (MQ 双轨 / 纯同步不引线程池). 单测 13 用例 (RocketMqBllTest 3 / ScriptMonitorBllAutoTriggerTest 5 / ScriptMonitorApiTest 5) 全过. e2e: DBA 验 tb_mq_message_record.message_status=1+message_id 不空 (业务直发成功) / B4 手动 triggerReport 同步直发 ~5-8 分钟 AI 跑完. 14 模块 BUILD SUCCESS. commit 1ed38ebae. 衍生 DEBT-011 (手动 monitorType=2 巡检仍拒 70014). | [link](./20260602_b7-auto-trigger.md) |
| 2026-06-02 | **STANDARD MEDIUM** | B8 setMonitorEnabled toggle 接口 + secUid 风格对齐 — POST /replay/script-monitor/setMonitorEnabled 单能力切换开关 (secUid + monitorType 0/1/2 + enabled 0/1) + AnchorUrlBll.updateMonitorSwitch (短路幂等 + 还原度暂禁 70014 + 监控位 70002 + LambdaUpdateWrapper 租户隔离 + countOpenSwitch 单 code 占用 + @Transactional + Feign 返 false 回滚) + ScriptMonitorController @NoRepeatSubmit + 类 Javadoc + B5 anchorBasicConfig 入参 anchorUrlUserId → secUid (复用 B7 加的 getBySecUidAndUser SPI) + 删 AnchorUrlUserFeign.getByIdAndTenantId 死代码 + 更新 wiki/frontend-api/script_monitor_b5.md 前端契约. 单测 7 用例全过 (AnchorUrlBllUpdateMonitorSwitchTest). e2e 9 ACs 全通 (含 monitorType=99 边界 + B5 secUid 改造验证). 14 模块 BUILD SUCCESS. commit 9119e08dd. | [link](./20260602_b8-set-monitor-enabled.md) |
| 2026-06-02 | **STANDARD MEDIUM** | reportStatus / batchReportStatus 加 monitorEnabled 字段 + 单接口 GET→POST 契约对齐 — POST /replay/script-monitor/reportStatus 替换原 GET (新建 ReportStatusBo) + BatchReportStatusBo.SourceItemBo 加可选 secUid + MonitorTypeStatusVo 加 monitorEnabled (0/1/null) + AnchorUrlUserFeign.listBySecUidsAndUser 批量 SPI (一次 IN 查 tb_anchor_url_user 三开关，带 userId/tenantId/isRemoveRecord=0 过滤) + ScriptMonitorBll.loadEnabledMap + pickMonitorEnabled 按 MonitorTypeEnum 选字段 + assemble/buildMonitorStatus 改签名. 单接口 breaking change，前端清单 script_monitor.md 同步更新. 14 模块 mvn compile SUCCESS, secrets gate PASS. | [link](./20260602_report-status-secuid.md) |
| 2026-06-02 | **STANDARD MEDIUM** | reportStatus / batchReportStatus summary 字段嵌套 Object 改造（契约 v1.3 对齐） — 新增 3 个 summary VO (QualityInspectionSummaryVo / FidelityMonitorSummaryVo / InteractionPatrolSummaryVo) + MonitorTypeStatusVo.summary 类型 String → Object + ScriptMonitorBll.buildListSummaryText → buildSummaryObject 按 monitorType 三分支装配 (entity 字段 interactionRate→effectivenessPercentage / summaryText→summary 响应层重命名). 同步: docs/2.6.01/apifox-import-script-monitor.yaml (reportStatus GET→POST + secUid + summary oneOf 三 schema + monitorEnabled 字段) + wiki/frontend-api/script_monitor.md (summary 子结构表 + 3 处示例响应) + 测试说明书 §5.5/§5.6 新增 15 用例 (8 reportStatus + 7 batchReportStatus). 规则沉淀: feedback-review-test-mandatory.md memory + CLAUDE.md §2 Review+Test 强制 dispatch 硬约束 + lifecycle.md Phase Matrix Review/QA 不再允许 inline. mvn compile SUCCESS, secrets gate PASS, OAS yaml 解析通过. 衍生 TECH-DEBT: B4 上线时另 4 项契约差异未对齐 (monitors[] 数组 vs 三 Object 字段 / isRead Boolean / batch items 包装 / 字段命名漂移). | (no spec — Edit-only, openspec 仍在上一行) |
| 2026-06-02 | **STANDARD HIGH** | summary 9 字段合并为 1 列 summary_json TEXT（destructive DDL）— sql/replay-30.sql DROP 9 列 (crash_count/slack_count/brand_damage_count/after_sales_count/score/speech_speed/deviation_summary/interaction_rate/summary_text) + ADD summary_json TEXT，information_schema 幂等守护，生产 JSON_OBJECT 重组迁移 + 测试 TRUNCATE。entity 9→1 字段；GenerateBll.buildSummary try-catch parse 后整段 summary 子对象 toJSONString() 直存原文；非法 JSON / 缺 summary key → null。SPI ScriptMonitorReportWriteService.finishReport(reportId,bodyId,summaryJson) / restoreOrFail(...,oldSummaryJson,...) 签名改造。ScriptMonitorBll.buildSummaryJson 直返 entity.getSummaryJson()。MonitorTypeStatusVo.summary Object→String。QualityReportDetailVo 删 4 count 顶层字段+加 summaryJson String。删 QualityInspectionSummaryVo/FidelityMonitorSummaryVo/InteractionPatrolSummaryVo 三 VO。2 ADR (1 字段 vs 3 字段 vs 9 列 vs 子表 / TEXT vs MySQL JSON 类型)。19 单测全过(测试基建复用上一批) + code-reviewer 3 MAJOR 全修(MQ mock 方法名 syncSendNormalMessageToTopic→syncSendAndDeliverToTopic / 补 @Mock AnchorUrlUserFeign / SQL Step 2 ADD COLUMN information_schema 守护). e2e 8 用例全过 (含数据迁移验证 4 count 完整保留)。commit 6a03d7404 → merge test 17527c46d. Approval Gate 经用户显式 confirm. | [link](./20260602_summary-flatten-to-json.md) |
| 2026-06-04 | **STANDARD MEDIUM** | 话术质检按直播间所属行业取提示词 — CueWordsFeign 新增 getCueWordByTradeAndType(tradeId, cueType) SPI（复用优化原文范式：行业父链回溯子→父优先 + 末尾兜底 trade_id=1 + 含 problem 字段不清空 + isDeleted=0 过滤）+ CueWordsProducer.listByTradeIdsAndCueType 实现（一次 IN 查全候选行业，命中策略内存端按 tradeIds 顺序找 sort 最小一条）+ ScriptMonitorGenerateBll 加 AnchorVideoFeign 依赖、新增 resolveTradeId(sourceType=0→tb_anchor_video.trade_id, sourceType=1→1L 兜底)、改 loadSingleCueWord 签名加 tradeId 异常文案含 tradeId+ 删旧 SPI getCueWordsByType / Producer.listByCueType 业务+测试全删（grep 验证无残留）+ 单测 12 用例全过（新增 testGenerate_fileSource_useFallbackTradeId 覆盖 AC-3 兜底）+ code-reviewer 4 MAJOR：MAJOR-1 isDeleted 回归已修（新 SPI 走 listByTradeIdsAndCueType 显式 eq isDeleted=0）/ MAJOR-2 null tradeId 契约不符已修（实现层永远 append 1L）/ MAJOR-3 import 未用 + MAJOR-4 useSourceType 硬编码均非本次改动行登记 DEBT-020/021/MINOR-5 类 Javadoc stale 已修。3 模块 mvn clean compile BUILD SUCCESS, secrets gate PASS. | [link](./20260604_script-monitor-prompt-by-trade.md) |
| 2026-06-05 | **STANDARD** | 话术质检提示词加租户定制维度 — CueWordsFeign.getCueWordByTradeAndType 改造为 CueWordsQueryBo 入参，新增 tenantId/scope/applyTo/accountType/syncScene 维度；CueWordsApi.getCueWordByTradeAndType 复用行业父链回溯 + 新建 CueWordsProducer.listByTradeIdsAndQuery 方法，WHERE 加 `tenant_id IN (bo.tenantId, 0)` + `ORDER BY tenant_id DESC, sort ASC` 确保同行业租户定制赢通用；ScriptMonitorGenerateBll.loadSingleCueWord 改调新 SPI 传递 report.tenantId；6 文件 + 1 单测文件跨 3 模块；单测 5 AC 全覆；code-reviewer/test-engineer 已 dispatch 返回 PASS；AiController bean 重名 fix（pre-existing）。| [link](./20260605_script_monitor_cue_words_tenant_dim.md) |
| 2026-06-05 | **PATCH #13** — `buildSummary` 提取从 JSON 改 markdown `<summary>` 标签 — AI 合并报告输出从 JSON (`{"summary":{...}}`) 改 markdown (`<summary>...</summary>` 标签包裹)，前端 + 测试已对齐。`ScriptMonitorGenerateBll.buildSummary` 重写为正则 `<summary>(.*?)</summary>` + `CASE_INSENSITIVE \| DOTALL` flags + `.*?` 非贪婪首匹配 + `.trim()` 去首尾；null/缺标签 → null + log.warn。删 fastjson2 `JSON/JSONObject` + hutool `ObjectUtil` import（仅 buildSummary 用），加 `Pattern/Matcher` + `SUMMARY_TAG_PATTERN` 常量。`ScriptMonitorReportEntity.summaryJson` Javadoc 改"markdown 文本片段"语义（字段名沿用历史命名，零迁移成本）。`ScriptMonitorReportBodyEntity` 类级+`content` 字段 Javadoc `HTML→Markdown` 修正（pre-existing 错注释顺手清）。测试：3 mock 工厂改造（aiResult 返 markdown / aiResultNoSummary 返合法 markdown 无标签 / 删 aiResultInvalidJson）+ 新增 2 helper（`setupHappyPathWithMergeContent` / `captureFinishReportSummary`）+ 重写 AC-7/8（提取 trim 文本 / 无标签→null）+ 加 AC-9~12（大小写不敏感/跨行/多标签首/trim）+ test-engineer 补 2 边界（null content / 空标签返 ""）；**20 test 全过**。code-reviewer PASS 2 MINOR（argThat 单行 240+字符 / null-content 路径无 log.warn），follow-up 已修：抽 `cueWordsMatcher(tradeId, tenantId, cueType)` 私有 ArgumentMatcher 工厂替换 8 处 argThat lambda（每行从 240+ 降到 ~95 字符）；null-content 早返补 log.warn 与缺标签路径对齐。20 test 仍全过。(no spec — PATCH mode) |
| 2026-06-06 | **STANDARD MEDIUM** | 互动巡检 T31-T36 后端能力 — ScriptMonitorPatrolGenerateBll 独立生成 Bll（弹幕 TableStore 时间窗查询 + ASR 词级时间戳归桶 + 串行切片 AI 多轮 + 合并 1 份 JSON 最终报告）+ PatrolPromptFiller 占位符填充工具（#{platform}/#{trade} contains 判断 + 兜底默认值"全平台"/"通用"）+ InteractionPatrolReportDetailVo + patrolReportDetail GET endpoint（含 confirmedAt 装配自 ScriptMonitorReadEntity.createDate）+ DanMuItemVo + TableStoreFeign.queryDanMuSearchData SPI + TableStoreApi @Override 委托现成 TableStoreBll 方法 + ScriptMonitorMqHandler tag=interaction-patrol 路由接通 + sql/replay-31.sql cueType=20/21 种子 INSERT + DEBT-011 closed（triggerReport 放开 monitorType=2 仅保留还原度暂禁）+ 合并提示词 schema 修正（totalUnits 由 10 硬编码改为占位符）。code-reviewer 2 CRITICAL + 4 MAJOR 全 fix（confirmedAt 漏装 / sql 同步 md / 单测断言旧行为 / tenantMismatch 单测缺失）。主 agent 走读发现 3 处 CRITICAL 时间基准 bug 后修复（弹幕查询时间窗 epoch ms 偏移 / buildBarrageText 视频内相对时间 vs UTC / ASR 空 vs PRD 8.1 矛盾）。全 14 module compile + 全 replay-ai 84/84 test PASS（含巡检 6 用例新加 testGenerate_danmuTimeIsRelativeToVideoStart_notUtc / generate_asrEmpty_marksNotApplicable）. 衍生 DEBT-023（时间窗单测须 mock 非零起始 epoch）/ DEBT-024（时间轴校准 PRD 语义未定义）. commits: 0526df6 / 20df53b / 309f78e / c6e55ca. push origin/feature/2.6.02（未合 test 单独发版）. | [link](./20260606_interaction-patrol.md) |
| 2026-06-07 | **PATCH #14** — `tb_script_monitor_report` setNull 静默失效修复 — `ScriptMonitorReportEntity` 给 `unavailableReason` / `summaryJson` / `reportBodyId` 三字段加 `@TableField(updateStrategy = FieldStrategy.ALWAYS)`（对齐 `VideoUserHotSubscriptionEntity` 先例），覆写 MyBatis-Plus 全局默认 NOT_NULL 策略 — 修复 `setUnavailableReason(null)` / `setSummaryJson(null)` 被静默从 UPDATE SQL 剔除导致 DB 旧值残留（reportId=4583835358719778816 现象：status=2 + summary 正常 + unavailableReason="ASR 数据为空..."）。涉及 3 处调用层（`ScriptMonitorBll:792` 重新 trigger / `ScriptMonitorReportWriteServiceImpl:53` finishReport / `:81` restoreOrFail GENERATED 恢复分支） + `GenerateBll.buildSummary:600/610` 间接经 finishReport 透传。**隐式语义变化（用户决策 A+C 接受）**：`restoreOrFail` FAILED 分支 + `markNotApplicable` 现在会清旧 summary_json + report_body_id（更符 NOT_APPLICABLE / GENERATE_FAILED 状态语义；reportBodyId 加 ALWAYS 避免出现"summary 清了但 MongoDB 旧正文指针残留"的脏字段）。Mockito 单测 10 个（entity 反射断言 × 3 + finishReport × 2 + restoreOrFail × 3 + markNotApplicable × 1 + errorMessage 截断/null 边界 × 1），@code-reviewer 两轮 PASS（mybatis-plus-annotation 3.5.6 源码验 `FieldStrategy.ALWAYS` + `TableField.updateStrategy()` 参数名）+ @test-engineer PASS。本机无 maven / mvnw wrapper 配置缺，编译走 reviewer 静态审 + QA `mvn -pl replay-ai test -Dtest=ScriptMonitorReportWriteServiceImplTest` 兜底；DB 行为走 QA manual verify SQL（reportId=4583835358719778816 三步：trigger 前 SELECT → 复跑 → 后 SELECT 期望 `unavailable_reason IS NULL`）。衍生 finding：`ScriptMonitorReportEntity` class 级 Javadoc 缺 `@author/@date`（pre-existing 存量，未在本次 scope 内修）。(no spec — PATCH mode) |
| 2026-06-08 | **STANDARD HIGH** | 巡检合并报告 JSON → Markdown 契约改造（mirror 质检 PATCH #13） — `ScriptMonitorPatrolGenerateBll.buildSummary` 从 `JSON.parseObject` 取 summary 子对象改为 `SUMMARY_TAG_PATTERN` 正则提 `<aifupan-data-block>` 标签内 trim 文本（删 fastjson2/JSONObject/hutool ObjectUtil import，加 java.util.regex.Pattern/Matcher）+ `InteractionPatrolReportDetailVo.summaryJson/reportContent` Javadoc 更新为 Markdown 语义 + 合并提示词 md 重写输出格式约定段（JSON → Markdown 区段 1 弹幕表格 / 区段 2 `<aifupan-data-block>` 标签包摘要 3-5 行） + sql/replay-31.sql cueType=21 INSERT 内容 Markdown 版 + **新建 sql/replay-32.sql UPDATE 覆盖现网已执行行**（含 tenant_id=0 防误更新租户私有；**必须先于代码部署执行**否则窗口期 summary_json 全 null 静默故障） + 前端契约 script_monitor.md 全局 summary 描述 + summary 子结构表 + patrolReportDetail 接口 summary/reportContent 行三处同步重写（删 JSON.parse 示例，改 Markdown 直渲）。Approval Gate 用户显式 approve；2 Nygard ADR：ADR-1 格式选型 Pure Markdown vs 保留 JSON+wrapper vs 双层 → 选 A；ADR-2 sql 策略 UPDATE vs 仅改 INSERT vs DELETE+INSERT → 选 A。空标签语义 `""` 与质检 PATCH #13 一致。@requirement-engineer + @system-architect + @test-engineer 全 dispatch；mvn test PASS 16/16（含反射式 buildSummary 8 边界 + AC-009 端到端 ArgumentCaptor 抓 finishReport 第 3 参数证伪 JSON）。Evidence Mapping Table + Manual Verify SQL（§A 幂等 + §B 实际脏数据 reportId 复跑 + §C 质检回归）已交 QA。**前端通知**：前端 owner 需在 B5 联调前知会 `patrolReportDetail` summary/reportContent 改 Markdown 格式。衍生 TECH-DEBT：测试类注释陈旧（INFO，下次顺手修）。 | [link](./20260608_patrol-summary-md-cast.md) |
| 2026-06-08 | **STANDARD HIGH** | B9 已读语义迁移主表 + 删 confirmRead/ConfirmRoleEnum/两张子表 — 已读状态从独立子表 (`tb_script_monitor_read` + `tb_script_monitor_role_confirm`) 迁移到 `tb_script_monitor_report` 主表列 (`is_read TINYINT + confirmed_at DATETIME`)；删除 `POST /replay/script-monitor/confirmRead` endpoint；detail 接口录制人首次打开时自动标记已读（fail-safe 独立事务）；`QualityReportDetailVo` 删 `confirmedRecords` 字段；`handleExistingAndReturn` 重置 is_read=0/confirmed_at=NULL；3 Nygard ADR（存储迁移 + 事务边界 + confirmed_at 保留）；14 AC 全 PASS；44 单测全 PASS；sql/replay-33.sql DDL 先 ADD COLUMN 后 DROP TABLE；code-reviewer/test-engineer dispatch；Approval Gate 用户显式 approve；衍生待确认：DDL 执行顺序评估（>50w 行用 gh-ost）/ 写已读失败告警阈值（建议 10 次/小时）。 | [link](./20260608_B9-read-semantics-to-report-column.md) |
| 2026-06-11 | **STANDARD HIGH** | 话术还原度 Slice A — 标准稿能力 + B3/B8 限闸解除：AiEnums.askType 18/19 重命名 FIDELITY_NON_CYCLIC_GENERATE/MERGE（数值零迁移）+ 25/26/27 新增 (FIDELITY_CYCLIC_GENERATE / FIDELITY_CYCLIC_MERGE / STANDARD_SCRIPT_GENERATE)。3 新 API：generateStandardScript 同步 AI 60s 不落库 / confirmStandardScript 按 secUid 联合定位软删旧+INSERT 新 / standardScriptDetail 按 secUid 查无稿返 hasScript=false+默认 280。ADR-2 final = (tenantId+userId+secUid) 联合定位（sql/replay-35.sql DROP anchor_url_user_id + DROP uk_tenant_anchor + ADD sec_uid VARCHAR(64) NOT NULL + ADD UNIQUE uk_tenant_user_secuid），**无占位 / 无回填 / 无幽灵行** — 否决 architect 初版 anchor_url_user_id=0 占位+回填方案。AnchorUrlBll:1116 B3 暂禁解除 + :2317 B8 暂禁解除，按 secUid 自动查标准稿（standardScriptId 入参可选），accountType=0 fallback bo.getAccountType()（B3 新主播场景 currentAnchorUser=null 不绕过 70004）。新建 StandardScriptService + StandardScriptServiceImpl @RequiredArgsConstructor 构造器注入（用户拒绝 lead-engineer ServiceImpl 子类必须字段注入 + 登 TECH-DEBT 借口，证伪后改 final 字段 + lombok）。sql/replay-34 5 套提示词 INSERT (tradeId=1/tenantId=0) + sql/replay-35 DDL。5 套提示词源稿改造（docs/2.6.01/REQ-2026-0508-话术还原度/）：占位符规范化（#{trade}/#{platform}/#{anchorAsr}/#{standardScript}/#{speechSpeed}/#{cycleDurationMinutes}/#{customFrameLinks}/#{customLinkFeatures}/#{referenceScript}/#{report1-3}）+ `<aifupan-data-block>` 标签统一（替换循环系列 `<话术还原度检验>`）+ "截取片段"→"整场" 语义（用户拍板不切片，整场喂 AI）。@code-reviewer 两轮：Round 1 2 CRITICAL（#1 parseMarkdownTable 双重 returnAiToken 拆解为外层 catch 唯一归还 + #2 B3 accountType fallback bo.getAccountType）+ 1 MAJOR（softDelete 去冗余 @Transactional 由 confirmStandardScript 外层覆盖）+ 6 MINOR（4 false alarm + 2 真修：AiEnums 注释去旧名字面量 + openspec §2.5.4/§7 stale 全面 secUid 重写）；Round 2 confirm 5 fix + 新发现 5 处 stale 全部已修（§5.5.1 Mermaid 改 StandardScriptService / §5.5.5 transaction 表 + 删 backfillAnchorUrlUserId blockquote / §5.2 70006 stale + 70005 改 secUid 触发 / §5.3 anchorUrlUserId→secUid / parseMarkdownTable @throws 补 70001/70008/70013）。@test-engineer 41 tests PASS（19 ScriptMonitorStandardScriptBll T21/T23/T24 + 12 AnchorUrlBll/B3 解禁 + 10 B8 解禁 + 既有 70014 暂禁测试改写 + 4 happy/edge 场景）。RedisWithholdVo redisId:Long/withholdId:String 命名反直觉踩坑→DEBT-021 OPEN（存量类，独立任务清理）。2 Nygard ADR：ADR-1 cueType 重命名（原地重命名 vs 废弃+新增连号 vs 仅新增不重命名 — 选 A，已 grep 零引用证据）+ ADR-2 secUid 主键定位（联合定位 vs anchor_url_user_id 占位+回填 vs 强制先保存直播间 — 选 A，secUid 全生命周期不变）。mvn clean compile BUILD SUCCESS 19.6s + mvn test 41/41 PASS。Approval Gate 用户显式 approve。Plan Deviation 5 段（ADR-2 中途重构 / Service 层抽离用户否决 TECH-DEBT 借口 / 两轮 review 累计 11 处 fix / lead-engineer 误判 AnchorVideoApi:68 真因 generic 未 install / DEBT-021 顺手登）。Non-Goals：T25/T26/T27 报告生成→Slice B；triggerReport monitorType=1 闸 / autoTriggerReport / MqHandler 分发 / fidelityReportDetail / 多页面适配→Slice C；anchorBasicConfig T07→独立 B4 批次；T05 公共权限统一化沿用现有 SPI 不新建。13 条 Pre-decided Constraints 全实装（cueType / 提示词 INSERT / 3 API / B3-B8 解禁 / Token 边界 / AI 模型 systemKv key 复用 / 自有账号 70004 / 6 BO/VO 构造器注入 / markdown 表格解析 / 回归质检巡检零影响）。15 文件改动（4 改 + 11 新）跨 4 模块 replay-common/replay-words/replay-api/sql。focus_card 演进 + DEBT-021 登账。secrets_linter PASS。 | [link](./Change__2026-06-11_12-33-48__restoreDegree-slice-a__openspec.md) |
| 2026-06-12 | **PATCH #15** — cueType=27 标准稿生成提示词输出 markdown 表格 → JSON 数组 + 反向修复 Slice A `parseMarkdownTable` → `parseJsonOutput` — docs/2.6.01/REQ-2026-0508-话术还原度/直播稿件转换成标准直播稿件提示词.md 重写为 JSON 数组示例（保留 6 个酒类示例，content 用 `\n\n` 分隔多段 + `\"` 转义双引号）；sql/replay-36.sql UPDATE tb_cue_words SET problem=<新 JSON 提示词> WHERE ask_type=27 AND tenant_id=0 AND trade_id=1 AND is_deleted=0（mirror sql/replay-32 巡检 markdown 改造范式，**部署前 SQL 优先**否则代码切 JSON 解析但 AI 仍输出 markdown → 70008）；ScriptMonitorStandardScriptBll 改 parseMarkdownTable → parseJsonOutput（JSON.parseArray + CODE_FENCE_PATTERN 主动剥离 ` ```json ... ``` ` 包裹 + JSON 反序列化失败/items 空抛 70008 + 仅抛不归还由外层 catch 统一处理 mirror Slice A Round 1 CRITICAL #1 修复），删 TABLE_ROW_PATTERN 正则 + ArrayList import；ScriptMonitorStandardScriptBllTest 21 tests（新增 AC-1b ```json``` 代码块剥离 + AC-6b 非法 JSON 即旧 markdown 抛 70008 模拟提示词回滚场景）。@code-reviewer 1 轮：0 CRITICAL + 1 MAJOR（类/方法 Javadoc 两处 "markdown 表格" 未同步改 "JSON 数组" → 修）+ 2 MINOR（sql 加 `-- Expected: 1 row affected` 注释 + CODE_FENCE_PATTERN 去掉无意义 Pattern.MULTILINE flag）全 fix。全 replay-words mvn test 43/43 PASS（21 std + 12 anchor + 10 monitor switch）。**Slice B 范围收紧连带**：Slice B openspec §4.1 Entity 补字段段 / §5.5.5 finishFidelityReport 段同步删除（用户拍板 score/deviationSummary/speechSpeed 全不写 + finishReport 公共方法不动 — mirror 质检/巡检完全一致）。衍生 finding：sql/replay-36 部署前必须 SOURCE 执行（否则 AI 输出新 JSON 但代码仍按 markdown 解析 → 70008 持续）。Slice B 报告生成路径用 cueType 18/19/25/26（4 个对比/合并），**不耦合** cueType=27（标准稿生成 T21）。(no spec — PATCH mode) |
| 2026-06-12 | **PATCH #16** — no-chinese-identifiers-deslop 红线落地 + 72 中文测试方法名英化 — 用户 2026-06-12 拍板"代码标识符禁中文（绝对红线）"，落 3 处：(1) CLAUDE.md §5 加 review CRITICAL 级硬约束行；(2) memory `feedback_no_chinese_identifiers.md` 完整 feedback（含 why + how to apply + 与 [[feedback-no-precedent-excuse]] 协同 + 测试方法名英文范式 `methodUnderTest_scenario_expected` + @DisplayName 中文双重表达）；(3) `.claude/scripts/harness/generate_hard_constraints.py` universal 段加 1 行（让未来 lead-engineer / test-engineer dispatch prompt 自动含本红线）。**72 中文测试方法名英化**（初始 grep 漏 23 处 _中间含中文 / 中文混合 模式 — code-reviewer Round 1 抓到补改）：15+9+8+7+6+2+2 (Slice A test-engineer 命名 49) + 4+2+3+3+10+1 (存量历史中文 23 占位补改) = 72 总；跨 7 Test 类 (ScriptMonitorBllTest 19 / ScriptMonitorBllAutoTriggerTest 6 / ScriptMonitorApiTest 5 / AnchorUrlBllTest 12 / AnchorUrlBllUpdateMonitorSwitchTest 10 / ScriptMonitorStandardScriptBllTest 17 / RocketMqBllTest 3)；保留 `@DisplayName` 中文（IDE / JUnit report 显示业务语义）。@code-reviewer 1 轮：7 CRITICAL (23 残留漏改，宽 regex `void\s+\S+?\s*\(` + 中文检测补抓全) + 2 MAJOR (1 存量 + 1 generate_hard_constraints.py --validate-ssot 4 处 pre-existing 漂移，非本 PATCH 引入留独立任务) + 2 MINOR (@author 占位修 + 命名质量验证通过) 全 fix。全项目 grep `[一-鿿]` in `void` declaration line 后 0 残留校验通过。mvn test 全 PASS 86/86 (43 replay-words + 40 replay-ai + 3 replay-common，0 failures 0 errors，2 skipped pre-existing)。Python 批量 string replace 实现（Path.read_text/write_text + 49+23 mapping table）。main code 0 中文标识符（grep 确认仅测试有问题）。规则永久落地：今后 @code-reviewer review 时若发现新增中文标识符直接 raise CRITICAL（红线级，非 MINOR/MAJOR）。 (no spec — PATCH mode) |
| 2026-06-12 | **STANDARD HIGH** | 话术还原度 Slice B — 报告生成核心 (T25/T26/T27)：新建 ScriptMonitorFidelityGenerateBll (~470 行 mirror 质检 ScriptMonitorGenerateBll 3+1 范式 — loadAiModelByKvKey + aiTokenWithholdFeign 预扣/settle/return + 4 次串行 AI 调用 + buildSummary `<aifupan-data-block>` 标签解析 + finishReport 三参 mirror) + 新建 StandardScriptFeign + StandardScriptApi + StandardScriptInfoVo（跨模块 SPI ADR-2 决策 — 否决扩 ScriptMonitorFeign / 直连 Dao） + 解 ScriptMonitorBll 3 处限闸（行 449-453 删 70014 第一道闸 + 行 480-483 替换为 standardScriptFeign.findValid 按 secUid 真实校验抛 70005 + autoTriggerReport:555 加 monitorType=1 路径） + ScriptMonitorMqHandler:107-108 fidelity tag 分发到 fidelityGenerateBll.generate + ScriptMonitorApi.autoTriggerForVideo 加 monitorType=1 + tryTriggerFidelityCapability（mirror scriptFidelityNum 范式，OrderEnums.commodityTypeCode.SCRIPT_FIDELITY_NUM 已就位）。speechMode 路由：speechMode=0 → cueType=18+19；speechMode=1 → cueType=25+26 + 占位符 `#{cycleDurationMinutes}` 填充。**Approval Gate 用户拍板收紧**（Plan Deviation #1）：Entity 不补 score/speechSpeed/deviationSummary 3 字段（永久 null）+ finishReport 公共方法签名不动（FidelityGenerateBll 直调三参 mirror 质检/巡检），focus_card 从 architect 初版 10 文件收紧到 7 文件。**Issue 2 触发独立 PATCH #15**（cueType=27 提示词改 JSON + Slice A parseMarkdownTable→parseJsonOutput 反向修复，零阻塞 Slice B）。**PATCH #16 用户立绝对红线**（代码标识符禁中文，落 CLAUDE.md §5 + memory + generate_hard_constraints.py，72 中文测试方法名英化跨 7 Test 类，新建 ScriptMonitorFidelityGenerateBllTest 12 case 全英文方法名 + @DisplayName 中文）。2 Nygard ADR：ADR-1 4 次 AI 调用全串行 (mirror 质检) vs 半并行 vs 全并行（选 A 首版正确性优先，60-90s 异步路径用户不感知；Slice D 可优化）；ADR-2 新建 StandardScriptFeign vs 扩 ScriptMonitorFeign vs 直连 Dao（选 A 符合 jiuyu 跨模块 Feign 强约束 CLAUDE.md §5）。@code-reviewer 两轮：Round 1 0 CRITICAL + 3 MAJOR（双重 anchorVideoFeign.GetByVideoId / 70013 错误码语义 / resolveInterKvKey-MergeKvKey switch case 0/2 死代码）+ 2 MINOR；MAJOR-1 修（mirror PatrolGenerateBll 范式 generate() 开头一次性取 video，secUid=video.getSecUid() 直接取，resolveTradeId(AnchorVideoInfoVo video) 改签名，删 resolveSecUid 私有方法）+ MAJOR-3 修（删 resolveInterKvKey/resolveMergeKvKey 方法 + loadInterAiModel/loadMergeAiModel 中间方法，抽常量 FIDELITY_INTER_KV_KEY/FIDELITY_MERGE_KV_KEY，generate() 直调 loadAiModelByKvKey）+ MAJOR-2 push back（mirror 质检 ScriptMonitorGenerateBll:224/259 既有 70013 convention，扩 StatusCode 超 Slice B Allowed Scope，errorMsg 已写精准描述）+ MINOR-1 push back（mirror 质检 generate() 138 行存量先例，mirror 优先于行数规范）+ MINOR-2/3/4 不修（comment_linter 注解夹缝 Javadoc 已知误判 + 正则 inline 保留现状）；Round 2 confirm 5 fix + 新发现 1 MINOR 孤悬 Javadoc（resolveSecUid 删了 Javadoc 残留 lines 386-395）已修。@test-engineer：4 个既有 ScriptMonitorBll*Test (Test/AutoTriggerTest/ReportStatusTest/SummaryTest) 构造器补 StandardScriptFeign 第 12 参 Mock + 替换 stale 70014 测试为新 Slice B 契约 (70005 无标准稿 + 不抛 70014 回归断言)；ScriptMonitorMqHandlerTest 替换 stale 跳过路径为 fidelityGenerateBll 派发新契约；ScriptMonitorApiTest 加 3 autoTriggerForVideo monitorType=1 case；**ScriptMonitorFidelityGenerateBllTest 新建 12 case** 覆盖 Happy Path 非循环/循环 + 标准稿查不到 + 第 2 次 AI 失败补偿 + buildSummary + 无标签 + finishReport 三参 + Token 边界 + cycleDurationMinutes=null + ASR 空 + settleAiToken 用 userId + autoTrigger withhold=null。全英文方法名 + @DisplayName 中文（红线遵守）。**mvn -pl replay-ai test 136/136 PASS** (0 failures, 0 errors, 2 skipped pre-existing DEBT-012)。Approval Gate 用户显式 approve（Issue 1/3/4 OK + 简化 Slice B 范围收紧自动包含在已 approve 中）。Plan Deviation 5 段（Approval 收紧 / 独立 PATCH #15 / 绝对红线 + PATCH #16 / Round 1 抓 3 MAJOR 处置策略 / test-engineer focus_card 缺 test 路径扩张）。Non-Goals：fidelityReportDetail GET 详情→Slice C；多页面适配→Slice C；score/deviationSummary 永久不写（用户已拍板）；DDL 无变更；提示词内容不动（已落 sql/replay-34 by Slice A）；finishReport 公共方法签名不动；ScriptMonitorReportEntity 不补字段；统一 AI 失败码留 Slice C；generate() 拆方法留 Slice C。7 文件改动跨 3 模块（replay-ai 主 / replay-words 1 新 Api / replay-generic 2 新 Feign+VO）。systemKv keys 用户已确认全部预配置：script_monitor_fidelity_inter_ai_model + script_monitor_fidelity_merge_ai_model；ASSISTANT_TYPE_FIDELITY=24 已就位（commit 796e54bb4）。secrets_linter PASS。 | [link](./Change__2026-06-11_18-57-48__restoreDegree-slice-b__openspec.md) |
| 2026-06-13 | **STANDARD MEDIUM** | 话术还原度 Slice C — 报告详情 + 多页面适配（**还原度功能完整闭环**）：新建 `FidelityReportDetailVo` 1:1 mirror `QualityReportDetailVo` 13 字段（不加 score/speechSpeed/deviationSummary — 用户 Slice B 拍板永久不写）+ `ScriptMonitorBll.fidelityReportDetail` 新方法（mirror qualityReportDetail：null/isDeleted=1 → 70012 / 跨租户 → 70011 / video=null log.warn 兜底 / 录制人 markReadIfNeeded fail-safe try-catch + log.warn / MongoDB 单查 reportBodyRepository.findByReportId / 13 字段装配）+ `ScriptMonitorBll.triggerReport` 加 sourceType=1+monitorType=1 guard（议题 E 决策：复用 70014 message 覆盖"还原度暂不支持上传文件场景"，不扩 StatusCode）+ `ScriptMonitorController` 新增 GET `/fidelityReportDetail` endpoint（@CrossOrigin/@Tag/@Operation）。**议题 D 决策（用户拍板缩减）**：视频分析（sourceType=1, sceneType=1）+ 文案预审（sourceType=1, sceneType=2）入口前端契约约定不传 monitorType=1 + 后端 DB 自然无该数据 → status=0，无新代码分支符合 YAGNI。`reportStatus`/`batchReportStatus` monitorType=1 装配复用现有 `buildSummaryJson`（monitorType 无关，零改动）。@code-reviewer 两轮：Round 1 APPROVED 0 CRITICAL + 0 MAJOR + 2 MINOR（#1 NOT_EXIST 双参带 message 不严格 mirror qualityReportDetail 单参 → 改单参 enum 默认；#2 comment_linter pre-existing 注解夹缝 Javadoc 工具缺陷不修）；Round 2 CONFIRMED FIXED + 7 case 落盘质量 CONFIRMED（全英文方法名红线遵守 + 13 字段断言完整 + Mockito mirror 既有 + import 类型修正合理）。@test-engineer 第一次 BOUNDARY_EXCEPTION（focus_card 缺 test 路径 — **Slice B Plan Deviation #5 同问题未汲取教训**），扩 focus_card 后 Re-dispatch PASS：7 case 落盘（fidelityReportDetail recorder/nonRecorder/notExist/softDeleted/crossTenant/markReadIfNeededFailSafe + triggerReport fidelity upload guard with sceneType=1）+ test-engineer 自检修 3 import 类型错（ScriptMonitorReportBodyEntity 非 Document / AnchorUrlInfoVo 非 AnchorInfoVo / FidelityReportDetailVo 漏 import）。**mvn -pl replay-ai test 143/143 PASS** (0 failures, 0 errors, 2 skipped DEBT-012 pre-existing；ScriptMonitorBllTest 36 含 Slice C 新加 7)。用户审 openspec 前置发现 3 处技术细节缺验证（fillAnchorFieldsFromUploadFile 不存在 / BusinessException 重载签名错 / 错误码表缺 70013 / AC-001 fail-safe 语义矛盾）→ 3 处 Edit 修复 + 1 处留 lead-engineer 兜底，避免 lead-engineer 按错 spec 写代码返工。MEDIUM 风险无 Approval Gate。Plan Deviation 5 段（architect openspec 技术细节缺验证 / focus_card 缺 test 路径重复未汲取教训 / test-engineer 自检修 import 类型 / Round 1 MINOR #1 严格 mirror 小 fix / MINOR #2 linter 误报不修）。Non-Goals：视频分析 + 文案预审入口（用户 2026-06-12 明确缩减范围）；Entity 不补字段；DDL 无；提示词不动；70013 → 新增 AI 失败码留 Slice B push back；FidelityGenerateBll.generate() 拆方法 push back；DEBT-021 RedisWithholdVo 命名整改。3 文件改动跨 2 模块（replay-ai 主 / replay-api Controller）。secrets_linter PASS。**至此话术还原度功能完整闭环：Slice A 配置 + Slice B 生成 + Slice C 详情/多页面**。 | [link](./Change__2026-06-12_23-27-59__restoreDegree-slice-c__openspec.md) |
| 2026-06-14 | PATCH | 话术还原度 Postman Collection 测试集（9 接口 + test 环境 environment + README）— `docs/2.6.01/REQ-2026-0508-话术还原度/postman/` 下 3 文件（516 行 collection + 79 行 environment + 148 行 README）。Collection 含 collection-level Bearer Auth + collection-level HTTP 200 断言 + 每接口 happy/边界断言 + 链路接力（Login token / generateStandardScript timeAxisScript / confirmStandardScript standardScriptId / reportStatus reportId 自动写入 environment）。Test 脚本断言含：R<T> 信封 / 业务码白名单 / 关键字段类型 / Snowflake 序列化（reportId/standardScriptId 为 string + 15-20 位数字防 long 精度丢失回归）/ 业务专属语义（generateStandardScript 响应不含 standardScriptId 验证不落库 / reportStatus 还原度 summary 是 Markdown 用 JSON.parse 应失败 / fidelityReportDetail status=2 时 isRead 自动置 1+confirmedAt 非空）。Environment 10 变量（3 必填手填：baseUrl/secUid/videoId；4 动态自动写：token/standardScriptId/reportId/generatedTimeAxisScript；3 默认值：username/password/userType；1 可选禁用：anotherTenantToken 跨租户测试）。README 含导入步骤 / 跑通顺序 / Collection Runner 注意（3.1→3.2 AI 等待 60-120s Postman delay 不够建议分两次）/ 9 边界用例对应 TC-07-18 / 7 已知限制排查 / Newman/traceId 串日志后续路线。归档前序 Slice A/B/C + PATCH #15/#16 已完整闭环还原度功能，本 PATCH 是配套交付物，零业务代码改动。(no spec — PATCH mode) |
| 2026-06-16 | **STANDARD MEDIUM** | 标准稿生成 JSON escape 容错重试 + SpEL WARN 修复 + cueType=27 提示词强化 — fastjson2 严格模式 + AI 非确定性输出耦合风险，catch `parseJsonOutput` 70008 触发 1 次重发（MAX_JSON_PARSE_RETRY_COUNT=1），二次成功则累加 token settle 一次；二次仍失败则 returnAiToken + 上抛。SpEL key 加单引号 `'generateStandardScript'` 修 EL1007E WARN 噪音（Greenplum NOREPEATSUBMIT AOP 字面量识别）。提示词 cueType=27 全 JSON 示例（escape 示例明示 `\"`）。sql/replay-37 UPDATE + 单测 AC-1~6 重试边界驱动（mock aiFeign 依次返合规/不合规内容）+ AC-7 手工 SpEL WARN 消失 + AC-8 提示词抽样。login_spec + explore_report 澄清模型回退是 QA DB 连错假象、P0 配置修复脱离本范围（DBA 线下走）；Plan Deviation 5 段（追 `×2.25` pre-existing 系数 + Allowed Scope 扩 TECH-DEBT.md + focus_card 中途扩 + settle 构造器参数化 architect Round 1 审+ 常量声明但未引用 lead-engineer Round 1 审）；登 DEBT-025 SpEL 存量问题（confirmStandardScript:73）+ DEBT-026 token 系数收敛（×2.25 而非 ×1.5）。Propose/Implement/QA 三轮 review 全过。| [link](./20260616_fix-generate-standard-script-json-escape.md) |
| 2026-06-17 | **PATCH** | 互动巡检 duration 单位错配修复 — `ScriptMonitorPatrolGenerateBll.java:271` 把 `video.getDuration()`（DB 列 `COMMENT '时长（秒）'`，秒）当作毫秒用，直接除以 `UNIT_WINDOW_MS=600_000ms`，导致 `unitCount = ceil(秒/毫秒)` 恒为 1，**任何 > 10min 视频报告只覆盖前 10 分钟**（30min 视频 1800/600000=0.003→ceil=1）。@debugger 多角度证实单位（DB 列注释 + AnchorUrlBll 格式化"{}秒" + AnchorVideoRseImpl `>=60` 过滤 + 样本值 59/33/167/49）；blast radius：全仓 15 处 `.getDuration()` 调用仅本处时间窗算法用法。修法：拆变量 `durationSec` / `durationMs` + `* 1000L` 转换 + 日志加 `durationSec={}` 防误导 + `MAX_TASK_DURATION_MS = 10min → 1h`（防修复后 24min 长视频任务超时）+ `durationSec=null/<=0` 兜底分支补 `log.warn`。单测：helper 单位 ms→sec 修正（6 处调用点）+ 新增 3 case（`generate_durationNull_fallbackToOneUnit` / `generate_durationJustOver10min_splitsIntoTwoUnits` 防 ceil→floor mutate / `generate_durationLongerThanUnitWindow_splitsIntoMultipleUnits` 30min e2e 含 ArgumentCaptor 3 切片 userContent 不同验证）；20 单测全 PASS（17 pre-existing + 3 新）。3 轮 review 闭环：lead round 1 PASS + reviewer 1 MAJOR + tester 3 missing → lead round 2 全 closed → reviewer round 3 APPROVE 0 finding（仅 2 MINOR 顺手 reflex）。**关键经验**：单测 helper 原传毫秒值与 DB 秒值不符，"巧合相等"导致 17 case 改单位前后都 100% 通过，无法捕获该 bug — 修复时同步修正 helper 单位+补 unitCount > 1 e2e 边界。衍生 TECH-DEBT 候选：4h+ 视频仍超 1h 总超时，需切片并发 / unitCount cap，本批次不修。 commit: 14032c223. (no spec — PATCH mode) |
| 2026-06-17 | **PATCH** | 全局校验报错文案去字段名前缀 — `GlobalExceptionHandler` 两处把"参数名: 文案"硬拼前缀的逻辑改成只输出文案：(1) `buildFieldErrorMessage`（@Valid / @RequestBody 走 `MethodArgumentNotValidException` / `BindException`）从 `error.getField() + ": " + error.getDefaultMessage()` → `FieldError::getDefaultMessage`；(2) `ConstraintViolationException` 分支（@Validated 类级 + @RequestParam）把 `propertyPath` 抽到 `fieldPaths` 局部变量只打日志，`errorMsg` 改为 `ConstraintViolation::getMessage` join；新加 import `jakarta.validation.ConstraintViolation`。用户报"互动巡检 'speechspeed: 语速不能低于 100 字/分钟' 参数名露给用户不友好"——根因是 `errorMsg` 走 dev/test 环境直返给用户，而 prod 走 `buildProdFieldErrorMessage` 本来就只回 `getDefaultMessage()`，本 patch 让 dev/test 与 prod 文案行为对齐。字段名仍打 log（`getFieldNames` / `fieldPaths` 两路）便于排错，可观测性不丢。新增 `GlobalExceptionHandlerTest.java`（首个 replay-api 真单测，4 case：AC-1 多字段 FieldError 拼接 / AC-2 ConstraintViolation 复现 stream 表达式 / AC-3 空 + null fieldErrors 默认文案兜底）；`mvn -pl replay-api test -Dtest=GlobalExceptionHandlerTest` 全 PASS。code-reviewer 报 1 MAJOR（`HandlerMethodValidationException` 分支未同步处理）—— 经判断该分支用 `MessageSourceResolvable::getDefaultMessage` 本就只返 message 文本，Spring 不会自动 prepend 字段名，实际行为已与改动一致，外科手术原则不动。衍生 TECH-DEBT 候选：(a) `ConstraintViolationException` 分支 inline stream 未抽方法，test 走"复现表达式"路线有 drift 风险；(b) Bo 校验 message 写法两派——含字段名（如 `"secUid 不能为空"`）vs 纯人话（如 `"语速不能低于 100 字/分钟"`），本批次不统一。focus_card: `.claude/runs/PATCH__strip-validation-field-prefix__2026-06-17_16:34:12/focus_card.md`. (no spec — PATCH mode) |
| 2026-06-17 | **PATCH** | 话术还原度标准稿 3-in-1 修复（test 环境复现，两轮迭代 round 2 用户反馈后改 UPDATE 模式）：(1) referenceScript 短输入导致 AI 输出 Markdown 解析失败 + 重试翻倍耗时（用户报"1分钟超时"，实测 75s = 36s × 2 触发 Nginx 60s 504）— `GenerateStandardScriptBo` / `ConfirmStandardScriptBo` referenceScript 字段加 `@Size(min=200, message="参考脚本不能少于 200 字符")`，治本拦截乱码 input；(2) `StandardScriptController:73` `@NoRepeatSubmit(key = "confirmStandardScript")` 缺单引号 SpEL EL1007E → 加单引号 `"'confirmStandardScript'"`（关闭 DEBT-025）；timeout 保持默认 5s（用户明确：所谓"5 分钟超时"指 Nginx `proxy_read_timeout`，非后端 NoRepeatSubmit；运维侧改）；(3) `Duplicate entry ... for key 'uk_tenant_user_secuid'` 1062 — 用户质疑"为什么每次都要软删"——grep 证实 `standardScriptId` 在当前代码无下游版本引用（`ScriptMonitorFidelityGenerateBll.java:209` 报告生成走 `findValid(tenant, user, secuid)` 按 secuid 重查活跃稿，不按 ID 取历史版本；`tb_anchor_url_user.standard_script_id` 字段全仓 grep 不到读者）→ 软删+INSERT 是过度设计。**改 UPDATE 模式**：`ScriptMonitorStandardScriptBll.confirmStandardScript` `findValid → 有就 updateById 内容字段（保留原 id）→ 没有就 INSERT 新稿`；同一 (tenant, user, secuid) 永远只一行，**根治 1062 + 表无限增长 + 不再依赖 uk 索引的 is_deleted 字段**。standardScriptId 跨 confirm 稳定（首次分配后不变）。code-reviewer round 2 MAJOR 顺手修：`updateById` 返 boolean 必接收，false 抛 RuntimeException 触发 `@Transactional` 回滚（CLAUDE.md §5 数据完整性延伸）；3 处 stale javadoc（Bll 类/方法 + Controller 方法）同步改"软删+INSERT"→"UPDATE/INSERT"。本批次 round 1 的 `physicalDeleteSoftDeleted` Service 新方法 + 3 个 InOrder ghost-cleanup 测试全 revert（UPDATE 模式下不需要）。**`softDelete` 既存方法保留**（接口 + Impl 均不删，加 javadoc 备注 "2026-06-17 起无活跃调用方"），关闭 DEBT-027（既无调用方，缺 tenantId 变无害）。**关键观察**：(a) 不是每个"暴露的设计问题"都该兜底——优先治本质疑设计意图。Round 1 我加 physicalDeleteSoftDeleted 是治标，round 2 用户问"为什么要软删"才暴露根本设计冗余。(b) `standardScriptId` 是死字段警示：未来引入"业务 ID" 时先验证下游真有版本引用需求，再决定要不要保留历史。test-engineer round 1 顺手修 pre-existing AiFeign→AiChatFeign 重命名（test 文件 3 行 mechanical 改动）；`mvn test -pl replay-words -am -Dtest=ScriptMonitorStandardScriptBllTest` 27 PASS（24 既存 + AC-2 改为 `hasExisting_updatesInPlace` 验证 updateById/standardScriptId 稳定/内容字段覆盖）。关闭 TECH-DEBT：DEBT-025 + DEBT-027。focus_card: `.claude/runs/PATCH__standard-script-3fix__2026-06-17_18:43:12/focus_card.md`. (no spec — PATCH mode) |
| 2026-06-19 | **PATCH** | 互动巡检弹幕透传用户画像字段给 AI — `DanMuItemVo`（replay-generic Feign VO）新增 `Boolean isNew` + `Long fansLevelCurrent`（`level` 原已存在）；`TableStoreApi.queryDanMuSearchData` 映射补 `setIsNew`/`setFansLevelCurrent`；`ScriptMonitorPatrolGenerateBll.buildBarrageText` 在昵称后拼接画像标签 `(等级X,粉丝团Y,新用户)`，三字段按非空动态拼接、全空不加括号、`Boolean.TRUE.equals` null 安全。**关键环境坑**：本机 `mvn` 默认挑到 JDK 26，JDK 23+ 默认关闭 classpath 注解处理器自动发现 + Lombok 1.18.32 不支持 JDK 26 → 整个 replay-generic 编译报 111 个"找不到符号"假错（全在未改文件 @Data/@AllArgsConstructor），**必须用 JDK 17 编**（`JAVA_HOME=.../ms-17.0.19` mvn ... → BUILD SUCCESS 0 error）。@code-reviewer：0 CRITICAL，1 MAJOR（`isNew` Lombok+Jackson 序列化陷阱）经核实不成立——`TableStoreFeign` 非 `@FeignClient`（本地 @Component 直调，无 JSON 序列化），且源端 `DanMuVo.isNew` 早用同写法，按外科手术不动；MINOR 均 pre-existing。@test-engineer：写 8 个 `buildBarrageText` 画像标签单测（全英文方法名 + @DisplayName 中文，逻辑自验通过），但**被 pre-existing 编译断点阻塞无法运行**——replay-ai 测试模块仍有 `AiFeign.chatCompletion`→`AiChatFeign` 迁移未完成（ScriptMonitorGenerateBllTest + ScriptMonitorPatrolGenerateBllTest 既有部分引用旧符号），衍生后台任务 task_35a4fc6c。commit 450b9ebb0（已合 feature/2.6.03 + 推远程）。**衍生 TECH-DEBT 候选**：(a) replay-ai 测试模块 AiFeign→AiChatFeign 迁移未完成致测试模块编不过；(b) 命令行编译需固定 JAVA_HOME=JDK17 否则撞 Lombok 假错。(no spec — PATCH mode) |
| 2026-06-21 | **STANDARD MEDIUM** | 互动巡检合并报告 AI 格式校验并纠正 — 合并多份报告 AI 输出格式常不规范（漏 aifupan 标签/被代码块包裹/表格跑歪），落库前增一次 AI 校验并纠正：cueType=28 提示词(内嵌格式规范，运营维护、与 cueType=21 同步)作 system + 合并报告作 user，单次调用；返回哨兵 `__FORMAT_OK__`(trim().equals 精确判定)用原文，否则视为纠正文并过代码兜底硬查(data-block 存在/无代码围栏)采用；配置缺/异常/纠正不合格一律降级输出原合并文(绝不阻断报告)。该校验/纠正 token 不计入 totalTokens、不结算(切片/合并不变)。设计经多轮对齐(ADR-1)：否决"Java 硬编码格式清单"(会过时)、否决复用 replay-words AiContentCorrectService(AI-check 不可靠+跨模块+dict 配置不符)；按用户反馈"格式经常错"将"校验+纠正两步"合并为单次调用(成本/延迟≤两步)；按用户选择把格式规范内嵌 cueType=28 DB 提示词(运营可维护、不必发版)而非运行时喂合并提示词。@code-reviewer 两轮：首轮 0 CRITICAL+1 MAJOR(后核实不成立)；单次版重审抓 MAJOR(哨兵 contains→trim().equals 防纠正文含哨兵子串误判)+补边界测试，全修。JDK17 全量 mvn -pl replay-ai test 36 PASS。⚠️上线前运营需配 systemKv script_monitor_patrol_format_ai_model + cueType=28 提示词，否则自动降级。commit ac2f17b5b。 | [link](./20260621_patrol-merge-format-ai-validation.md) |
| 2026-06-22 | **PATCH** | 删除主播两个 bug 修复（跨 replay-words + replay-ai 2 模块 4 文件）：(1) **软删主播未关闭三开关** — `AnchorUrlBll.updateUserAnchor` 软删分支(isRemoveRecord==1)原仅释放资源位(回写 tb_user_property_type)，未把行上 `isScriptQualityInspection/isScriptFidelityMonitor/isInteractionPatrol` 三字段置 0，导致主播恢复后开关复活、与资源位统计不一致 → 在置 isRemoveRecord 落库前(updateById 走 BeanUtils 全拷)对三字段 set 0，仅 isRemoveRecord==1 时触发(Objects.equals null 安全，不误伤普通修改路径)。(2) **行业取错来源** — 话术质检/互动巡检/话术还原度三功能 resolveTradeId 在 VIDEO 来源下原直接用录制记录 `video.getTradeId()`(tb_anchor_video.trade_id)，改为按 secUid+userId+tenantId 经 `AnchorUrlUserFeign.getBySecUidAndUser` 取直播间维度 `tb_anchor_url_user.trade_id`(该字段已按 systemTradeId 优先算好，AI 侧无需再判 system/ai 优先级)，查不到/为空兜底 1L；质检文件上传来源(sourceType=1)未动。巡检为显式构造器(加参+赋值)，质检/还原度 @AllArgsConstructor(加 final 字段)；巡检/还原度 resolveTradeId 签名由 (video) 改为 (video, report)。@code-reviewer PASS(0 CRITICAL/0 MAJOR/1 MINOR 构造器 Javadoc 缺 @param 已补)。@test-engineer：质检/巡检/还原度各 2 个 resolveTradeId 测试(取直播间 trade_id 忽略 video.tradeId + 回退 1L) + AnchorUrlBllSoftDeleteTest 2 个(ArgumentCaptor 验三开关置 0 + 非软删不误伤)，JDK17 mvn -pl replay-ai/-am 与 replay-words/-am test 全 PASS。focus_card: `.claude/runs/PATCH__2026-06-22_15:35:14/focus_card.md`. (no spec — PATCH mode) |
| 2026-06-24 | PATCH | `AiAgentQuotaServiceImpl#getAiTokenNum` 性能优化 — 新增 `UserPropertyBll#getSharedPropertyByCode(userId, code)`（owner 经 `userFeign.getUserIdOrParentId` 解析，单 code 直读 total/used 缓存共 2 次 Redis GET，复用 `isPropertyHaveAiToken` 预扣 owner 口径），替换原 `getUserProperty(userId)` 全量拉取所有资产类型（Feign + 按商品类型逐个 Redis GET）再内存过滤 aiTokenNum；balance/reserve/settle 现读同一 owner 缓存，口径一致。语义：缓存缺省返回 total=0/used=0 VO，等价原 null（下游按 0 处理）。JDK17 离线编译 replay-order,replay-api PASS。 | (no spec — PATCH mode) |
| 2026-06-29 | **STANDARD MEDIUM** | 三类监控报告 detail 接口（`qualityReportDetail` / `patrolReportDetail` / `fidelityReportDetail`）读权限放宽到云空间分享场景 — 原仅同租户可读，新规：「同租户 OR `tb_anchor_video.upload_status == 1`（视频已上传云空间，主播主动公开分享）」可读，否则抛 70011。3 个方法镜像改造：`loadSingleVideo` 顺序前移到权限校验之前，引入 `sameTenant \|\| publicShared` 双闸口。写已读规则（`hasConfirmPermission` 要求录制人本人 + 同租户）不动 → 跨租户分享接收方天然不触发 `markReadIfNeeded`；列表/状态接口（`reportStatus` / `batchReportStatus`）不动；错误码 70011 沿用；无 DDL / Feign / 错误码变动。Controller 三个端点 Javadoc 同步更新（quality 末轮 review 漏改已补，含权限说明 + 去 B9 已删字段）。`AnchorVideoInfoVo.uploadStatus` 在 sourceType=1 的 bridge VO 里恒为 null，`Objects.equals(null, 1) == false` → 跨租户 + sourceType=1 自动命中拒绝分支（AC-5 零代码自满足）。@code-reviewer PARTIAL（0 CRITICAL / 1 MAJOR / 3 MINOR）：MAJOR 已修；MINOR #2（fidelity sourceType=1 log.warn 位置）决定不修——reportId 19 位 Snowflake 不可枚举 + log 服务端可见、不返用户 + quality/patrol 同类 log 位置一致；MINOR #4（comment_linter 误报）跨目录预存。@test-engineer 新建 `ScriptMonitorBllReadPermissionTest.java` (+466/-0)，21/21 全绿（3 方法 × 7 AC，含 AC-5 `verify(sensitiveWordsFeign, never()).getUploadFileInfo(any())` 关键回归），JDK 21 0.721s。base 分支 `feature/jdk-21-mvc`（非 master）。 | [link](./20260629_shared-report-readable.md) |
| 2026-06-30 | **STANDARD MEDIUM** | anchorBasicConfig 接口补全 tb_basic_settings 字段（含 livingMode）— 前端反馈"添加或修改主播基础设置信息时，直播间模式没正确更新"。根因排查：`/addOrUpdateAnchor` 写库正常，但 `GET /replay/script-monitor/anchorBasicConfig` 读路径只查 `tb_anchor_url_user`、漏读 `tb_basic_settings`，导致响应 VO 从 `BasicSettingsBaseDto` 继承的 12 字段（accountType / premiereDate / accountStage / accountWaterLevel / accountFlow / livingTarget / livingModality / marketing / optimizeDirection / learning / livingMode / anchorSituation）永远是 null。方案 B 单点修：(1) `replay-generic` 新建 `BasicSettingsFeign.getByAnchor(secUid, userId, tenantId)` SPI；(2) `replay-words` 新建 `BasicSettingsApi` 实现，复用既有 `basicSettingsProducer.getBySourceUser`；(3) `ScriptMonitorBll.anchorBasicConfig` 构造器加 `BasicSettingsFeign`，在原响应基础上 try-catch fail-safe 合并 12 字段。`BeanUtils.copyProperties` 用 `ignoreProperties` 显式排除 BasicSettingsVo 自有 7 字段（id/sourceId/sourceType/userId/tenantId/createDate/updateDate），双重意图：(a) 防污染 vo 的 anchor 维度同名字段；(b) createDate/updateDate 两侧类型不兼容（BasicSettingsVo=LocalDateTime，AnchorUrlUserVo=Date），BeanUtils 静默跳过但显式排除让意图明确。sourceType=1 场景、其他 4 处 getBySecUidAndUser 调用方（质检/巡检/还原度 generate + ScriptMonitorApi）、`AnchorUrlUserApi.getBySecUidAndUser` 实现均不动。@code-reviewer PASS（0 CRITICAL / 1 MAJOR / 3 MINOR）：MAJOR-1 类型不兼容注释未说明已修（补充"createDate/updateDate 类型不兼容"注释）；MINOR-2 `WordsEnum.basicSettingsType` 首字母小写命名遗留（reviewer 自认本次无法绕开，需独立 TECH-DEBT 登记）；MINOR-3 `R.error(30000)` magic number 是预存代码不修；MINOR-1/2 (linter false positive) 跨目录预存。@test-engineer 用户主动 kill 未执行（有 6 AC × 单方法 × 6 测试的完整设计，见 openspec §7）。base 分支 `feature/jdk-21-mvc`，本次合并目标 `release`。 | [link](./20260630_fix-anchor-config-livingmode.md) |
| 2026-07-13 | PATCH | `AiAgentReplayServiceImpl#videoList` 视频列表补采集统计字段 — `VideoItemVo` 新增 `totalOnlineNum`（累计场观人数,String）/`observationNum`（场观人数,String）/`totalBarrageNum`（弹幕总数,Integer）/`onlineMaxNum`（最高在线人数,Integer）4 字段。原 `getVideoHasChartDataMap` 全链（`SocketCollectMessageProducer(Impl)` → `SocketCollectMessageBll` → `SocketCollectMessageLogic(Impl)` → `AiAgentReplayService(Impl)`）返回类型由 `Map<String,Boolean>` 改为 `Map<String,SocketCollectMessageVo>`：producer 查询从只 select `totalOnlineNum` 扩为 select 4 字段并 `BeanUtils.copyProperties` 装配 VO；`videoList` 端 `hasChartData` 改为 `chartData != null` 判定（能查出即 true），并回填 4 字段。查询 filter 与原 sibling 一致（未加 isDeleted，保持既有语义）。JDK21 reactor 编译 replay-generic,replay-words,replay-api BUILD SUCCESS。focus_card: `.claude/runs/PATCH__2026-07-13_video-item-socket-fields/focus_card.md`. (no spec — PATCH mode) |
| 2026-07-07 | PATCH | `AiAgentReplayServiceImpl#videoDashboard` 无看板兜底截图 — 当巨量/蝉妈妈看板取数为空（`loadDashboardInfo` 返回 null）时，查 `tb_data_screenshot` 中 `sourceId=videoId` + `screenshotStatus=3`（AI 识别完成）+ 视频归属租户(`video.getTenantId()`，兼容跨租户已分享视频) 的记录，按「screenshotCode + aiContent」组装为新 VO `DashboardScreenshotVo`，塞进 `VideoDashboardVo.screenshots`（有看板数据时不填）。新增 `DashboardScreenshotVo`（generic）+ `VideoDashboardVo.screenshots` 字段 + 注入 `DataScreenshotService`。JDK17 离线 reactor 编译 replay-generic,replay-api BUILD SUCCESS。focus_card: `.claude/runs/PATCH__2026-07-07_video-dashboard-screenshot/focus_card.md`. (no spec — PATCH mode) |
| 2026-07-18 | PATCH | AiAgentReplayController 新增福袋列表接口 /bless-bag/list（tenantId + videoIds 必填 IN 查询，BlessBagLogic + BatchQuery 分批全量，仅租户隔离不验用户） | (no spec — PATCH mode) |
| 2026-07-24 | **STANDARD HIGH** | 管理后台 `POST /user/pageListNew` 的 `UserListVo` 新增三个只读字段 `userPowerConsume`（用户累计算力消耗）/ `tenantPowerConsume`（所属租户全体累计）/ `latestRemark`（最新跟进内容 + 时间 + 跟进人）。算力口径取业务扣费台账 `tb_user_property_details`（弃用 `total_use_quantity` 与 `tb_ai_token_use_record`，见 ADR-1），新建 `tb_user_power_rollup` / `tb_tenant_power_rollup` / `tb_power_rollup_watermark` 三张离线汇总表，由 XXL-Job `timingUpdatePowerConsume` 增量累加刷新（净额口径、按 `parent_user_id` 归"发生时租户"永不漂移、主键水位线 + 1 小时安全滞后，见 ADR-3）；列表侧在单 MySQL 库前提下由 `pageListNew` SQL 双 `LEFT JOIN` rollup 两表零额外往返带出，不新增跨模块 Feign（见 ADR-2）；跟进记录由批量 IN 装配。涉及 replay-order / replay-power / replay-api，不动 replay-generic。3 条 ADR 随本 openspec 整体归档（Change intent，未挂载 @architecture-curator）。 | [link](./20260724_userlist-power-remark.md) |
| 2026-07-23 | PATCH | AI Agent 开放接口 `dataSourceType` 新增 4=自己录制+云空间（并集）：`videoList` 为 `(tenantId+userId 本人录制) OR (uploadStatus=1 已分享跨租户)`；`anchorDetail` 子账号为 `(user_id=本人 OR is_auto_upload_cloud=1)`（主账号自己录制即租户级、天然覆盖）；`searchAnchorList` XML 新增 `dataSourceType==4` 分支 `(a.user_id=#{userId} or a.is_auto_upload_cloud=1)`，三处 tenant_id 隔离不变。3 个 QueryBo 注释同步。JDK21 reactor 编译 BUILD SUCCESS，@code-reviewer APPROVED（0 CRITICAL/MAJOR）。focus_card: `.claude/runs/PATCH__2026-07-23_10-00-00/focus_card.md`. (no spec — PATCH mode) |
| 2026-07-29 | PATCH | 修复单测编译错误：InfluencerListTest 误用 bo 变量改 isNull()、TenantInfluencerRow 补第3参、ScriptMonitorBll 5个测试补 BasicSettingsFeign mock、跨租户用例补 video stub | (no spec — PATCH mode) |
| 2026-07-27 | PATCH | `computeRoiForVideo` 新增 ROI 显示条件控制：仅自有账号(accountType=0)且流量结构≠1 才计算 ROI，同行账号或流量结构=1 直接 return null | (no spec — PATCH mode) |
| 2026-08-01 | PATCH | AI Agent balance 接口新增 nextResetDate（aiToken 下次重置时间）+ packageExpireDate（非增量包套餐过期时间） | (no spec — PATCH mode) |
| 2026-08-10 | PATCH | Data Hub 数据开放：新增 /internal/data-hub 7 个 api-key 接口（租户映射/成员名单/登录状态/业务账号/授权状态/分析状态/订单对账）+ sql/replay-44.sql 索引 + 对外 API 文档 v1.0/v1.1 | (no spec — PATCH mode) |
| 2026-08-18 | Standard(MEDIUM) | AI Agent 爆款选题模块：§6.19 新增 `tenantScopeExtract` 租户口径提取授权开关（默认 false 行为不变，true 走 assembleVideoItems + 缓存键并入 tenantId 分片）；新增 §6.21 `POST /internal/ai-agent/hotsearch/analytics/overview` 爆款选题拆解聚合（门槛线/集中度/互动结构/day-week 自适应趋势/时长桶含 avgEngagement/发布热力/Top10 达人/Top20 #标签/租户文案可用条数，60s 缓存按 tenantId 分片，样本上限 2 万条 + truncated 标记）。新增 DAO `listHotSearchClipMetrics` + `countTenantExtractedVideoIds`。JDK21 编译 BUILD SUCCESS，@code-reviewer PASS（0 CRITICAL），scratchpad 驱动验证 6 组边界全通过。focus_card: `.claude/runs/Change__2026-08-18_hotsearch-analytics/focus_card.md` | (no spec — 跨会话协作任务，契约见 docs/ai-agent-replay-openapi.md §6.21) |
