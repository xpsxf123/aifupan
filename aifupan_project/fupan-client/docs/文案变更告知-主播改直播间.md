# 文案变更告知：主播 → 直播间

> 变更范围：仅将下列指定页面中「用户可见文案」里的「主播」改为「直播间」。
> 原则：按前后文确认，**不做全局盲替换**。代码注释、变量名、接口字段名（如 `anchorName`、`secUid`）以及未列出的页面均保持不变。

---

## 一、直播间列表 tab（`src/views/modules/dataAnalysis`）

| 文件 | 位置 | 改动前 | 改动后 |
|------|------|--------|--------|
| `dataAnalysis/index.vue` | 搜索框 placeholder（2 处） | 请输入直播间名称 | 请输入直播间名称 |
| `component/common/search.vue` | 搜索框 placeholder | 请输入直播间名称 | 请输入直播间名称 |
| `component/recordHistory.vue` | 录制历史列表「主播」列名 | 主播 | 直播间 |
| `component/recordHistory.vue` | 彻底删除确认弹窗 | 彻底删除后主播信息无法恢复，请确认操作 | 彻底删除后直播间信息无法恢复，请确认操作 |
| `component/mixin.js` | 直播间列表「主播」列名 | 主播 | 直播间 |
| `component/common/anchor.vue` | 岗位默认展示文案 | 主播 | 直播间 |

### 基础设置（`dialog/basicSettings.vue` 复用 `addCompere/common/compereForm.vue`）

| 位置 | 改动前 | 改动后 |
|------|--------|--------|
| 「直播间名称」表单项（label + placeholder） | 直播间名称 | 直播间名称 |
| AI 自动监控标题 | AI自动监控主播话术/弹幕互动项 | AI自动监控直播间话术/弹幕互动项 |
| 负面话术质检说明 | 每场主播下播后…主播话术… | 每场直播间下播后…直播间话术… |
| 话术还原度监控说明 | 主播有没有…主播是不是随意发挥…监控主播整场直播…防止主播随意发挥 | 直播间有没有…直播间是不是随意发挥…监控直播间整场直播…防止直播间随意发挥 |
| AI 弹幕互动巡检说明 | 主播有没有及时回复…主播的互动…主播的回复…防止主播随意发挥 | 直播间有没有及时回复…直播间的互动…直播间的回复…防止直播间随意发挥 |

---

## 二、添加直播间（`src/views/modules/addCompere`）

| 文件 | 改动前 | 改动后 |
|------|--------|--------|
| `index.vue` | 小Tips：添加主播后，如主播开启直播 | 小Tips：添加直播间后，如直播间开启直播 |
| `index.vue` | 还可以添加主播数 | 还可以添加直播间数 |
| `index.vue` | 目前已经添加主播数量 | 目前已经添加直播间数量 |
| `index.vue` | 添加主播授权数量不足，是否立即扩容？ | 添加直播间授权数量不足，是否立即扩容？ |
| `common/douyin.vue`（添加抖音） | 在下方粘贴要检测录制的一个主播账号 | 在下方粘贴要检测录制的一个直播间账号 |
| `common/kuaishou.vue`（添加快手） | 在下方粘贴要检测录制的主播账号ID | 在下方粘贴要检测录制的直播间账号ID |
| `common/addCompereStatusDialog.vue` | 您已添加 N 个主播，请删除部分主播后 | 您已添加 N 个直播间，请删除部分直播间后 |
| `common/compereForm.vue`（共享表单） | 请选择主播行业 | 请选择直播间行业 |

---

## 三、复盘直播（`src/views/modules/replay`）

| 文件 | 位置 | 改动前 | 改动后 |
|------|------|--------|--------|
| `component/compere.vue` | AI整场/AI切片/短视频切片 左侧搜索框 placeholder | 输入直播间名称搜索 | 输入直播间名称搜索 |

---

## 四、云空间 - 整场复盘（`src/views/modules/replay/online`）

| 文件 | 位置 | 改动前 | 改动后 |
|------|------|--------|--------|
| `online/online-video.vue` | 搜索项 label | 直播间搜索 | 直播间搜索 |
| `online/online-video.vue` | 搜索框 placeholder | 输入直播间名称 | 输入直播间名称 |

---

## 五、本次未改动（按上下文判断保留）

以下「主播」**未做替换**，请确认是否符合预期：

1. **代码注释/文档注释**中的「主播」（如 `// 获取主播列表`、`@description 主播录制配置表单`）——保留开发语义，未改动。
2. **`addCompere/oldIndex.vue`**（旧版添加页，文件名 `oldIndex`，疑似已废弃未使用）——未改动。
3. **`dataAnalysis/component/allAnchors.vue`** 删除/录制相关提示：「将删除直播间」「主播正在录制中，请停止录制后再删除」等——未在你列出的「列名/搜索框/基础设置」范围内，未改动。
4. **`dataAnalysis/hotList/index.vue`**（优秀账号热度榜）的「直播间名称」「请输入直播间名称」——不属于直播间列表 tab 主列表，未改动。
5. **`replay/record-list.vue`** 的「直播间搜索」「请输入直播间名称」（短视频切片顶部筛选，`showItems: ['replayShort']`）——你列出的复盘直播项是「输入直播间名称搜索」（左侧 `compere.vue`），此筛选项未明确列出，未改动。
6. **`aiMixin/common.js`**（AI 违规/运营助手收藏列表）的「直播间搜索」——不在本次页面范围内，未改动。
7. **`dataAnalysis/index.vue`** 中「缺少主播标识…」等 `$message` 提示——未在列出的条目内，未改动。

> 如需将上述「未改动」项一并替换，请告知，我再补充处理。
