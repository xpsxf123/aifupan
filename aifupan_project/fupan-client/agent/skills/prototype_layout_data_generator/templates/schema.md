# 原型布局数据（Markdown Code）

说明：页面原型布局数据，以 json 代码块形式提供。

```json
{
  "页面": "{page_id}",
  "区域": [
    {"名称": "筛选栏", "类型": "form", "字段": [{"prop": "keyword", "label": "关键词", "type": "string"}]},
    {"名称": "表格", "类型": "table", "列": [{"prop": "name", "label": "名称"}]}
  ]
}
```
