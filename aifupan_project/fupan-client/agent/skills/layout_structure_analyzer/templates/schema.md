# 布局结构（Markdown Code）

说明：页面布局要求，以 json 代码块承载。

```json
{
  "项目名称": "{project_name}",
  "分析时间": "{date}",
  "布局要求": [
    {
      "页面ID": "{page_id}",
      "页面名称": "{page_name}",
      "整体布局": {"类型": "上下布局|左右布局|自由", "结构": []},
      "主体区域布局": {"类型": "垂直布局|水平布局", "间距": "16px", "区域顺序": []},
      "栅格": {"系统": "24列", "间距": "16px"}
    }
  ]
}
```
