# 表单验证（Markdown Code）

说明：字段验证规则，使用 json 代码块。

```json
{
  "项目名称": "{project_name}",
  "分析时间": "{date}",
  "表单验证规则": [
    {
      "表单ID": "{form_id}",
      "表单名称": "{form_name}",
      "所在页面": "{page_id}",
      "验证规则": [
        {
          "字段名": "{field}",
          "字段标签": "{label}",
          "验证时机": ["提交|变更"],
          "规则列表": [
            {"规则类型": "格式|长度|范围|必填", "格式名称": "邮箱|手机号", "错误提示": "{message}"}
          ]
        }
      ]
    }
  ]
}
```
