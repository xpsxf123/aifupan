# 通用：双层记忆与知识图谱工作流

## 目标

用 `INDEX.md` 快速定位、用 `wiki/` 存细节，并在每次交付后回写知识。

## 标准流程

1. 查 INDEX：定位概念与依赖关系
2. 选页面：只加载最少量 wiki 页面
3. 产出：生成回答/文档/代码/清单
4. 回写：把新知识写入 wiki，并更新 INDEX 关系与权重
5. 反思：必要时写入 DREAMS，并沉淀策略到 POLICIES

