# 未对接接口页面报告

## 说明

本报告根据路由配置（`src/config/menu.js` + 开发环境 `src/config/dev/component-menu.js`）梳理页面入口，并结合页面代码特征（如 `mock`/`setTimeout`/写死列表数据/“模拟API”字样）识别“仍未对接真实接口或仍包含 mock 数据”的页面。

## 一、生产菜单（menu.js）

| 路由 | 页面文件 | 状态 | 证据/备注 |
| --- | --- | --- | --- |
| `/my-schedule-performance/performance` | `src/views/MyScheduleAndPerformance/PersonalPerformance.vue` | 未对接（mock） | 页面内写死“用户信息 (Mock)”与固定字段值 |
| `/my-schedule-performance/schedule` | `src/views/MyScheduleAndPerformance/PersonalSchedule.vue` | 未对接（mock） | `generateMockData()` 生成排班数据 + 写死日期 `2026-01-24` |
| `/staff-management/schedule-detail` | `src/views/StaffPerformanceAndSchedule/StaffSchedule/Detail/index.vue` | 部分未对接（写死） | `userInfo` 写死（注释提示应从后端获取） |
| `/live-room-performance` | `src/views/LiveRoomPerformance/index.vue` | 部分未对接（趋势图 mock） | 趋势图数据使用固定 `initialTrendChartData` |

## 二、开发菜单（component-menu.js，仅 DEV 环境）

| 路由 | 页面文件 | 状态 | 证据/备注 |
| --- | --- | --- | --- |
| `/component-docs/sms-code-input` | `src/views/ComponentDocs/SmsCodeInput/index.vue` | 示例（mock） | `mockSendApi` 使用 `setTimeout` 模拟发送 |
| `/component-docs/data-ranking-list` | `src/views/ComponentDocs/DataRankingList/index.vue` | 示例（mock） | `mockApi.list` 使用 `setTimeout` + `Array.from` 生成列表 |
| `/component-docs/permission-select` | `src/views/ComponentDocs/PermissionSelect/index.vue` | 示例（mock） | 写死 `permissionTreeData` |
| `/component-docs/global-search` | `src/views/ComponentDocs/GlobalSearch/index.vue` | 示例（mock） | 文案注明“组件内使用模拟数据（setTimeout）” |

## 三、src/views 内命中但未在路由中注册（可能是遗留/示例）

| 文件 | 类型 | 证据/备注 |
| --- | --- | --- |
| `src/views/LiveRoomPerformance/LiveRoomListDemo.vue` | 演示/示例 | `generateMockData()` + `setTimeout` 模拟加载/搜索/重置 |
| `src/views/DepartmentAndStaff/GroupDepartment/index.vue` | 业务页面（未注册） | 存在 `setTimeout(() => clearValidate(), 0)`（非接口 mock，仅异步校验） |
| `src/views/DepartmentAndStaff/LiveRoom/constants.js` | 遗留常量（未引用） | 存在 `mockData` + “模拟API” `setTimeout` |
