# ADR Directory

One file per architectural decision, named `NNNN-<slug>.md`. The canonical index is the **ADR List** table inside [`../index.md`](../index.md) — append a row there when adding a new ADR.

- Template: [`template.md`](template.md)
- Numbering: scan `NNNN-*.md` here, take `max + 1`.
- Format: follow `architecture-decision-records` SKILL.
